# Synthetic validator fixtures, not credentials.
#
# Commit e159b4e introduced tests/memoryValidatorCore.test.mjs with two
# credential-SHAPED strings exercising the memory validator's §8.4 secret
# patterns (import/memory policy, eval category ③). The strings were
# invented for the test and never belonged to any system; the follow-up
# commit reshaped them so they no longer resemble real secrets, but the
# introducing commit is immutable history and stays pinned here.
#
# Scope: exactly these two findings in that one commit — nothing wider.
e159b4ed85a3ba5e3f9c286b8a1f207933aa7254:tests/memoryValidatorCore.test.mjs:generic-api-key:121
e159b4ed85a3ba5e3f9c286b8a1f207933aa7254:tests/memoryValidatorCore.test.mjs:jwt:123

# Synthetic extraction-eval fixture, not a credential.
#
# Commit c5d6331 introduced tests/fixtures/memoryExtractionEval.mjs with an
# AWS access key id SHAPE invented for the §12.2 extraction eval's category ③
# fixtures: the prefix, then a body spelling FIXTURE and padded with zeros, so
# it can never collide with an issued key. The fixture set later moved to
# lib/memoryExtractionEvalFixtures.ts without it, so only the immutable
# introducing commit still carries it.
#
# The value itself is deliberately not quoted here. Writing it out put the
# same shape in this file, which the scan then reported against the commit
# that added it -- pinning a finding we had just created.
#
# This is why the scheduled full-history scan failed while every PR and push
# scan passed: those scan a commit range that does not include c5d6331.
#
# Scope: exactly these two findings in that one commit — nothing wider.
c5d63312a625450918df5bf8f4a0c32e575243b1:tests/fixtures/memoryExtractionEval.mjs:aws-access-token:501
c5d63312a625450918df5bf8f4a0c32e575243b1:tests/fixtures/memoryExtractionEval.mjs:aws-access-token:511

# Svix webhook secrets in a test, not credentials.
#
# Commit 32d95e6c introduced tests/emailSuppressionCore.test.mjs with six
# `whsec_`-prefixed strings: one is Svix's own published verification vector
# (docs.svix.com), the other was invented for the rotation test. Neither ever
# signed anything outside this file. gitleaks scans a line at a time and
# cannot tell a documented vector from a live secret, which is the correct
# behaviour for a scanner -- so the follow-up commit stopped writing the
# shape (the vector is joined from its parts, the round-trip secrets are
# derived) rather than teaching the scanner to ignore it.
#
# The introducing commit is immutable history and stays pinned here.
#
# Scope: exactly these six findings in that one commit — nothing wider.
32d95e6c9296554d43fa0969fa243eff7791d3ad:tests/emailSuppressionCore.test.mjs:generic-api-key:164
32d95e6c9296554d43fa0969fa243eff7791d3ad:tests/emailSuppressionCore.test.mjs:generic-api-key:171
32d95e6c9296554d43fa0969fa243eff7791d3ad:tests/emailSuppressionCore.test.mjs:generic-api-key:199
32d95e6c9296554d43fa0969fa243eff7791d3ad:tests/emailSuppressionCore.test.mjs:generic-api-key:216
32d95e6c9296554d43fa0969fa243eff7791d3ad:tests/emailSuppressionCore.test.mjs:generic-api-key:217
32d95e6c9296554d43fa0969fa243eff7791d3ad:tests/emailSuppressionCore.test.mjs:generic-api-key:245
