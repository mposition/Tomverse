name: Auto PR to Develop

on:
  push:
    # Opt-in by merge target, not by purpose.
    #
    # This was `branches-ignore` with a list of namespaces that open their own
    # PRs. That list can only name the main-targeting prefixes someone has
    # already been surprised by, and on 2026-08-15 it missed one: a branch
    # carrying a `.github/dependabot.yml` change -- a file read only from the
    # default branch -- was pushed for a `main` PR, and this workflow opened
    # #573 against develop first. Two PRs for one branch, one of them based
    # where the change would have done nothing.
    #
    # Under opt-in an unrecognised branch gets no PR, which costs one manual
    # `gh pr create`. Under opt-out it got a PR against the wrong base with
    # auto-merge enabled.
    #
    # The prefix still says who made the branch and why; the `to-develop`
    # segment says where it is going:
    #
    #   claude/to-develop/image-generation   docs/to-develop/release-policy
    #   codex/to-develop/fix-picker          fix/to-develop/ime-submit
    #
    # This filter is the cheap first cut. `scripts/auto-pr-branch-policy.mjs`
    # is the authority and runs in the job below -- a glob is not testable and
    # its `**` semantics are subtle, so the decision that matters lives in a
    # module with a branch matrix behind it.
    branches:
      - "to-develop/**"
      - "**/to-develop/**"

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: auto-pr-to-develop-${{ github.ref }}
  cancel-in-progress: true

jobs:
  auto-pr:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      GH_TOKEN: ${{ secrets.GH_AUTOMATION_PAT }}
      BRANCH: ${{ github.ref_name }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      # The filter above and this step answer the same question, and this one
      # is the answer. Keeping both means a widened glob still gets refused
      # here, and it puts the reason in the log for a skipped run -- a silent
      # skip and an unwanted PR are the same failure from the outside: nobody
      # can tell which rule applied.
      - name: Decide from the branch's merge target
        id: target
        run: node scripts/auto-pr-branch-policy.mjs "$BRANCH"

      # Deliberately not falling back to ${{ github.token }}: a pull request
      # opened with GITHUB_TOKEN does not trigger workflow runs, so the auto-PR
      # would sit there with no checks at all. A missing PAT has to be fixed,
      # not worked around -- this step just says so in one line instead of
      # leaving gh's "set the GH_TOKEN environment variable" hint behind.
      - name: Verify the automation token is available
        run: |
          if [ -z "${GH_TOKEN}" ]; then
            echo "::error::GH_AUTOMATION_PAT is empty for this run. Check that the secret exists and that this event can read repository secrets."
            exit 1
          fi

      # Two questions, not one: is this branch's work already on develop, and
      # if not, does it change anything?
      #
      # This used to be a single `git diff origin/develop...HEAD` after a
      # `git fetch origin develop --depth=1`, and that combination is what
      # opened PR #467 for a branch whose work was already merged -- a pull
      # request whose merge commit changed no file at all.
      #
      # `actions/checkout` above fetches the full history (`fetch-depth: 0`).
      # Fetching develop again with `--depth=1` then re-shallowed the
      # repository, cutting develop's tip off from its parents. Once develop
      # had advanced past the point this branch last merged it, `merge-base`
      # had nothing to walk, so the three-dot diff did not report "no changes"
      # -- it failed outright:
      #
      #     fatal: origin/develop...HEAD: no merge base
      #
      # and the `if` read that failure as the "there is a diff" branch. A
      # broken measurement became a positive answer.
      #
      # So: fetch develop with its history, ask the containment question
      # directly, and treat an unanswerable merge-base as an error rather than
      # as a yes.
      - name: Skip if the branch has nothing to merge into develop
        id: diff-check
        if: steps.target.outputs.create == 'true'
        run: |
          set -uo pipefail
          git fetch origin develop
          if git merge-base --is-ancestor HEAD origin/develop; then
            echo "HEAD is already contained in develop; nothing to open a PR for."
            echo "has_diff=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          if ! git merge-base origin/develop HEAD >/dev/null 2>&1; then
            echo "::error::No merge base between origin/develop and HEAD. The repository is shallow or the branch is unrelated to develop; refusing to guess whether a PR is needed."
            exit 1
          fi
          if git diff --quiet origin/develop...HEAD; then
            echo "has_diff=false" >> "$GITHUB_OUTPUT"
          else
            echo "has_diff=true" >> "$GITHUB_OUTPUT"
          fi

      # Two outcomes, and the difference between them is what the next step
      # reads: `created=true` only when this run's own `gh pr create` opened the
      # pull request. Finding one already open is not the same event, and the
      # workflow must not treat it as one.
      - name: Create PR to develop if missing
        id: create-pr
        if: steps.target.outputs.create == 'true' && steps.diff-check.outputs.has_diff == 'true'
        run: |
          set -uo pipefail
          existing=$(gh pr list --head "$BRANCH" --base develop --state open --json number --jq '.[0].number')
          if [ -n "$existing" ]; then
            echo "PR #$existing already open for $BRANCH -> develop; leaving its merge settings alone."
            echo "created=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          if ! gh pr create \
            --head "$BRANCH" \
            --base develop \
            --title "$BRANCH -> develop" \
            --body "Auto-created by the Auto PR to Develop workflow. Auto-merge was armed once, at creation; if it is turned off it stays off."; then
            echo "::error::gh pr create failed for $BRANCH -> develop."
            exit 1
          fi
          number=$(gh pr list --head "$BRANCH" --base develop --state open --json number --jq '.[0].number')
          if [ -z "$number" ]; then
            echo "::error::gh pr create reported success but no open PR for $BRANCH -> develop can be found."
            exit 1
          fi
          echo "created=true" >> "$GITHUB_OUTPUT"
          echo "number=$number" >> "$GITHUB_OUTPUT"
          echo "Created PR #$number for $BRANCH -> develop"

      # Armed once, at creation, and never again.
      #
      # This step used to run on every push to the branch: it looked up the open
      # PR and called `gh pr merge --auto` on whatever it found. Auto-merge is a
      # setting a person can turn off, and re-arming it on the next push makes
      # that decision temporary without saying so. On 2026-09-05 that is what
      # happened to #1256 -- auto-merge was verified off, a commit was pushed,
      # this workflow turned it back on, and the pull request squash-merged into
      # develop while its author was still working on it. Nobody approved that
      # merge; a workflow re-armed a switch someone had already thrown.
      #
      # So the condition is the creation event, not the existence of a PR. The
      # `if` is the guard -- the step does not look a PR up for itself, it is
      # handed the number the create step opened. A run that found a PR already
      # open never reaches here, which is the whole rule: off stays off.
      #
      # A pull request that touches .github/workflows/** cannot have auto-merge
      # enabled by this token: GitHub refuses the mutation with
      #
      #   refusing to allow a Personal Access Token to create or update
      #   workflow ... without `workflow` scope (enablePullRequestAutoMerge)
      #
      # That is a deliberate restriction and the right one -- a change to the
      # gate should not be able to arrange its own merge. But failing the job
      # over it puts a red check on every workflow PR for a reason that is not
      # a defect, and a check that is red for non-defects stops being read.
      #
      # So the refusal is reported and tolerated; anything else still fails.
      # The pull request is unaffected either way -- only auto-merge is, and
      # such a PR is merged by a human on purpose.
      - name: Arm auto-merge on the pull request this run created
        if: steps.create-pr.outputs.created == 'true'
        env:
          PR_NUMBER: ${{ steps.create-pr.outputs.number }}
        run: |
          set -uo pipefail
          # `gh pr create` above passes no `--draft`, so this cannot be true
          # today. It is kept because `gh pr merge --auto` fails on a draft, and
          # adding `--draft` there should change what this step does rather than
          # turn the job red.
          if [ "$(gh pr view "$PR_NUMBER" --json isDraft --jq '.isDraft')" = "true" ]; then
            echo "Auto-merge not armed: PR #$PR_NUMBER was created as a draft."
            exit 0
          fi
          if output=$(gh pr merge "$PR_NUMBER" --squash --auto 2>&1); then
            echo "$output"
            exit 0
          fi
          echo "$output"
          if printf '%s' "$output" | grep -q "without \`workflow\` scope"; then
            echo "Auto-merge not armed: this PR changes a workflow file, so it must be merged by a human."
            exit 0
          fi
          exit 1
