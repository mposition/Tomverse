name: Secret History Scan

on:
  pull_request:
  push:
    branches: [main]
  schedule:
    - cron: "45 22 * * 0"
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read

# Superseded runs on the same PR (or the same branch/schedule) are cancelled
# so a burst of pushes does not queue up scans of commits nobody will merge.
# Keyed per PR number where there is one, and per ref otherwise, so a main
# push or the weekly full-history scan can never cancel a PR's scan or
# another PR's. The event name keeps the pull_request and push scans of the
# same ref in separate groups -- they scan different commit ranges.
concurrency:
  group: secret-history-scan-${{ github.event_name }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  gitleaks:
    name: Scan repository history for secrets
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: gitleaks/gitleaks-action@v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # The scan should fail the check but does not need to write PR comments.
          GITLEAKS_ENABLE_COMMENTS: "false"
