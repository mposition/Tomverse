# B-4 provider 예산 운영 설정 기록 — staging, 2026-09-08

`docs/ops/voice-provider-budget-rollout.md` §6의 기록입니다. 절차는 §5.

**이 기록은 staging 절반입니다. B-4는 이 기록으로 해결되지 않습니다** — §7의
완료 조건 1·2가 양쪽 환경을 요구하고, production은 §5에서 확인된 이유로 아직
충족할 수 없습니다(아래 "production" 절).

- **실행일(UTC)**: 2026-09-08
- **실행자**: @mposition
- **staging deploy SHA**: `d79d3c8673db4a9f5f4cd109bc99c50c21e182e3`
- **production deploy SHA**: `500ce79f0ae46b021dac9b7c96c35f0587a8d617` (branch `main`)
- **선택한 안**: **A (보수적 pilot)**
- **선택 근거**: 사용량 관측이 없는 첫 pilot에서 노출을 작게 제한하고, 관측이
  생긴 뒤 올린다.

---

## 관측한 것과, 보고받은 것

이 둘을 섞지 않습니다. 검사기는 **설계상 값을 출력하지 않으므로**, 숫자 자체는
설정한 사람만 알고 있고 도구가 확인해 준 것이 아닙니다.

| 사실 | 출처 |
|---|---|
| 두 변수가 **존재**하고, 양의 정수이며, `month >= day` | 검사기 출력 (아래) |
| 그 값이 **1,800 / 27,000**이라는 것 | **실행자 보고** — 도구로 확인되지 않음 |
| 변수 **이름**이 staging 서비스에 등록됨 | Railway 서비스 설정(이름만, 값 미포함) |
| 적용된 규칙이 production rule임 | 검사기 첫 줄 |
| flag 값과 갱신 시각 | DB 조회 |
| readiness 두 dependency | 공개 `/api/ready` |

## 설정한 값

| 환경 | 일 | 월 |
|---|---|---|
| staging | 1,800 | 27,000 |
| production | — (설정하지 않음, 아래) | — |

월 = 일 × 15. 매일 일 한도를 전부 쓰면 15일째에 월 예산이 소진되고 남은 보름은
멈춥니다. 의도된 pilot 제한입니다(rollout 문서 §3.4).

## 적용된 규칙 (검사기 첫 줄)

- **staging**: `production rule (NODE_ENV=production)`
- **production**: 미확인 — 검사기가 존재하지 않음

`next start`가 production 규칙을 준다는 것이 **추론에서 관측으로** 바뀌었습니다
(rollout 문서 §0.1.1).

## Voice flag 상태

**설정 전** — `SELECT value FROM "AppSetting" WHERE key = 'feature.voiceInputEnabled';`

```
0 row(s) returned
```

행 없음 = off. `voiceInputEnabledFromValue`가 `value === "true"`이므로
`undefined`는 off입니다. **`VOICE_INPUT_KILL_SWITCH`가 비어 있다는 사실과는
무관하게** 이 조회로 확인했습니다.

**설정 후** — `SELECT key, value, "updatedAt" FROM "AppSetting" WHERE key = 'feature.voiceInputEnabled';`

```
feature.voiceInputEnabled | true | 2026-09-08T09:17:19.565Z
```

**production**: 변경하지 않음. 이 절차는 production flag를 켜지 않습니다.

## 검사기 출력

### 1차 — 변수 반영 전 컨테이너 (버리지 않고 남깁니다)

컨테이너 `d5363a0cd768`. 변수를 추가한 **직후**, 그 변수를 받지 않은 채 이미
떠 있던 컨테이너에서 실행했습니다.

```
Voice provider usage budget (seconds) — production rule (NODE_ENV=production)

  VOICE_PROVIDER_SECONDS_PER_DAY: MISSING — missing
  VOICE_PROVIDER_SECONDS_PER_MONTH: MISSING — missing

  Verdict: NOT usable — voice input would refuse every request.
```

**설정 오류가 아닙니다.** 컨테이너 환경은 기동 시점에 고정되고, 이 컨테이너는
변수 추가보다 먼저 떠 있었습니다. 이 출력을 남기는 이유는 두 가지입니다 —
순서(변수 → 재배포 → 검증)를 지켰다는 증거이고, **flag를 이 시점에 켰다면
`/api/ready`가 503이 됐을 것**이라는 fail-closed의 실증입니다.

### 2차 — 재배포 후 (판정 대상)

컨테이너 `d7eac571a91e`, deploy `d79d3c8673db4a9f5f4cd109bc99c50c21e182e3`.

```
Voice provider usage budget (seconds) — production rule (NODE_ENV=production)

  VOICE_PROVIDER_SECONDS_PER_DAY: set — ok
  VOICE_PROVIDER_SECONDS_PER_MONTH: set — ok

  Verdict: usable — both values are set and consistent.
```

### production

**실행하지 못했습니다.** `scripts/check-voice-provider-budget-env.mjs`가 `main`에
없습니다.

## staging `/api/ready`

flag를 켠 뒤, 같은 deploy에서 읽었습니다. Cloudflare Access 뒤에 있어 실행자가
브라우저로 읽었습니다.

```json
{
  "ok": true,
  "checks": {
    "database": true,
    "securityEnvironment": true,
    "providerBudgets": true,
    "imageProviderBudget": true,
    "voiceProviderBudget": true,
    "voiceModelPrice": true,
    "searchProviderBudget": true,
    "emailSendingIdentity": true,
    "emailSnapshotKeyring": true,
    "emailUnsubscribeKeyring": true,
    "emailBusinessIdentity": true
  },
  "traceId": "53034543-0a85-4bc7-b2f4-8cc61fb5443e"
}
```

- `checks.voiceProviderBudget`: **true**
- `checks.voiceModelPrice`: **true**
- `ok`: **true**
- HTTP status: **200**

**`voiceModelPrice`가 판정을 받은 것은 이번이 처음입니다.** §6.1.4의 검사는
flag가 켜져 있을 때만 동작하고, 이 배포 전까지 어느 환경에서도 flag가 켜진 적이
없었습니다. #1280이 register에 넣은 관측(US$0.001616, audio 요율 $3.00/1M)이
운영에서 처음으로 검사를 통과했습니다.

---

## production

**설정하지 않았고, 지금은 설정하지 않는 것이 맞습니다.**

`main`에는 **voice 관련 파일이 하나도 없습니다** — `lib/voiceProviderBudget.ts`
자체가 없고, 검사기도 npm script도 없습니다. `main`은 `develop`보다 3,339 커밋
뒤에 있습니다.

그러므로 지금 production에 두 변수를 넣으면:

- 그 변수를 읽을 코드가 없습니다
- `/api/ready`가 그것에 대해 아무 말도 하지 않습니다
- `npm run check:voice-provider-budget-env`는 script가 없어 실행되지 않습니다
- 대가로 효과 없는 production 재배포가 한 번 일어납니다

**env-first의 이득이 성립하지 않습니다.** 그 규칙은 "flag를 켜기 전에 변수가
있어야 한다"인데, production에는 켤 flag를 읽을 코드가 없습니다.

**production 설정은 `develop` → `main` 릴리스와 함께** 합니다. 그때 코드와 변수가
같이 들어가고, 같은 시점에 검사기로 검증까지 됩니다.

---

## 판정

- [x] staging 두 변수 설정됨 (검사기 `usable`)
- [x] staging `/api/ready` 두 항목 healthy
- [ ] production 두 변수 설정·검사기 통과 — **릴리스 대기, 이 기록의 범위 밖**
- **판정(통과/조건부/실패)**:
- **서명**:

## B-4 상태

**미해결.** rollout 문서 §7의 완료 조건 1·2가 양쪽 환경을 요구하고, production
절반은 위 사유로 아직 충족 불가능합니다. `docs/policy/voice-input.md` §14의
B-4는 "결정·코드 완료, 환경별 실제 한도 설정 대기" 그대로입니다.

Production Voice flag는 B-4·B-5·B-6으로 계속 꺼져 있습니다.
