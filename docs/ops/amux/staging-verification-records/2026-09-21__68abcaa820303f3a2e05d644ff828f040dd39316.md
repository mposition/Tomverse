---
record: staging-verification
checklist: docs/ops/amux/staging-checklist.md
templateRevision: 2026-09-21
checklistSourceSha: 68abcaa820303f3a2e05d644ff828f040dd39316
runType: exploratory
resultScope: flag-off-source-migration-capture-only
deploymentId: 7cde1aff-2592-4b52-b854-c921b98fcb54
artifactDigest: sha256:6a3f0bbf0716cad0e2469d1513cca20c7f99feb2a1f4a6b0ba9f5fa10138ee54
appliedMigrations: 20260921120000_amux_attempt_budget, 20260921123000_amux_incident_mode, 20260921124000_amux_planning_resources, 20260921160000_amux_agent_review_approval
environment: railway-staging-amux-validation-isolated-service
deploySha: 68abcaa820303f3a2e05d644ff828f040dd39316
startedAtUtc: 2026-09-21T13:21:25.860Z
completedAtUtc: 2026-09-21T13:29:08.481Z
executor: Codex
approver: mposition
approvedAtLocalDate: 2026-09-22
result: 통과
activationAllowed: false
frozen: true
digest: 80aa6ad18fa7d11fb31feced06482f0d
stateDigest: 6c9d19d2ce29946e101e102d0f3e2bfc6a7bac87f410668369ccecf803ee1ff5
capturePath: docs/ops/amux/staging-verification-records/2026-09-21__68abcaa820303f3a2e05d644ff828f040dd39316.capture.json
---

# AMUX staging 검증 실행 — 2026-09-21 / 68abcaa

이 기록의 사람 판정 `통과`는 2026-09-22 mposition이 답한 **제시된 관측 범위**
(배포 SHA, migration, flag-off 설정, 읽기 전용 캡처)에만 적용된다. 57개 체크리스트
전체 통과, #6 승인 경로 활성화, PR 병합 또는 production 배포 승인이 아니다. 실행하지
않았거나 직접 관측하지 못한 항목은 `미기록`이다. `completedAtUtc`는 Railway 배포
성공 시각이며, 승인자는 날짜만 제공했으므로 서명 시각을 만들어 넣지 않았다.

## 실행 환경

| 항목 | 값 |
|---|---|
| 환경 | staging |
| 배포 SHA (전체 40자리) | `68abcaa820303f3a2e05d644ff828f040dd39316` |
| template revision | 2026-09-21 |
| 시작 (UTC) | 2026-09-21T13:21:25.860Z (Railway deployment 생성) |
| 종료 (UTC) | 2026-09-21T13:29:08.481Z (Railway deployment `SUCCESS`) |
| 실행자 | Codex (관측·기록 전사); 사람 판정은 mposition |
| AMUX_ENABLED | Railway 서비스 변수 `0`으로 설정; runtime 응답 미관측 |
| AMUX_EXECUTE | Railway 서비스 변수 `0`으로 설정; runtime 응답 미관측 |
| AMUX_EXECUTION_API_ENABLED | Railway 서비스 변수 `0`으로 설정; runtime 응답 미관측 |
| AGENT_APPROVAL_ENABLED | Railway 서비스 변수 `false`로 설정; runtime 응답 미관측 |
| worker catalog 상태 | 서비스 변수 목록에 없음 |
| executor config 상태 | 서비스 변수 목록에 없음 |
| execution_ready | 미기록 (private 서비스의 routing API 미호출) |
| 공개 도메인 | 없음; 검증 서비스만 staging DB를 공유 |

## 항목별 결과

체크리스트의 A–H 구획을 그대로 기록한다.

각 항목은 `pass`, `fail`, `n/a`, `미기록` 중 하나를 사용한다.

| 구획 | 항목 | 결과 | 증거 | 후속 티켓 |
|---|---|---|---|---|
| **A. Source identity와 feature-off** | | | | |
| | staging이 보고하는 deploy SHA가 기록의 40자리 `deploySha`와 정확히 같다. | pass | Railway deployment `7cde1aff-2592-4b52-b854-c921b98fcb54`의 source SHA 및 자동 capture의 `deployment_revision` 모두 `68abcaa820303f3a2e05d644ff828f040dd39316` | - |
| | `TOMVERSE_AMUX_ENABLED`가 unset 또는 `1` 이외 값인 상태를 관측한다. | 미기록 | Railway 서비스 변수는 `0`으로 설정했으나 runtime 응답을 직접 관측하지 않음 | - |
| | `TOMVERSE_AMUX_EXECUTE`가 unset 또는 `1` 이외 값이다. | 미기록 | Railway 서비스 변수는 `0`으로 설정했으나 runtime 응답을 직접 관측하지 않음 | - |
| | `TOMVERSE_AMUX_EXECUTION_API_ENABLED`가 unset 또는 `1` 이외 값이다. | 미기록 | Railway 서비스 변수는 `0`으로 설정했으나 runtime 응답을 직접 관측하지 않음 | - |
| | worker catalog와 executor command 설정이 activation 없이 주입되지 않았다. | pass | 검증 서비스의 Railway 변수 이름 목록에 worker catalog 및 executor command 변수가 없음 | - |
| | server routing 응답의 lifecycle gate가 `execution_ready=false`다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| **B. 완전 비활성 상태** | | | | |
| | `TOMVERSE_AMUX_ENABLED`가 off일 때 orchestrator는 성공적으로 종료하며 internal URL 또는 AMUX sync secret을 요구하지 않는다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | off 상태에서 worker runtime registration이 발생하지 않는다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | off 상태에서 task claim이 발생하지 않는다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | off 상태에서 execution attempt가 새로 시작되지 않는다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | off 상태에서 durable delivery가 새로 생성되지 않는다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| **C. Selection-only 상태** | | | | |
| | scheduler가 queue를 읽고 global-priority selection을 계산할 수 있다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | selection log가 선택과 실행을 분리해 표시한다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | selection-only 동안 task owner/revision이 selection 자체 때문에 변하지 않는다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | execution attempt가 생성되지 않는다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | durable delivery가 생성되지 않는다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | executor configuration이 없어도 selection-only service가 실행된다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| **D. Fencing과 durable execution contract** | | | | |
| | worker runtime identity가 instance id + generation으로 fenced된다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | stale/replacement generation은 heartbeat/settlement 권한을 잃는다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | execution start와 durable delivery가 하나의 atomic commit이다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | live delivery receipt retry는 같은 receipt를 유지한다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | receipt lease 만료 뒤 pull은 새 receipt를 발급한다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | 만료되거나 교체된 receipt의 ACK는 거절된다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | worker fence loss 뒤 executor가 취소되고 stale settlement가 전송되지 않는다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | fenced/ambiguous worker는 같은 process에서 자동 재등록되지 않는다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| **E. Audit와 recovery** | | | | |
| | claim decision에는 scheduler와 worker-router evidence가 함께 남는다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | execution start/settle/recovery가 canonical admin audit chain에 연결된다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | expired current execution recovery만 Todo로 반환되고 stale attempt는 변경하지 못한다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | expired owner reservation recovery가 기존 routing evidence를 삭제하지 않는다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | audit integrity verification이 staging evidence 범위에서 정상이다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| **F. Advanced planning guard와 자동 증거** | | | | |
| | incident freeze 중 새 claim/start는 거절되고, freeze 전에 만들어진 durable delivery와 heartbeat/settle/recovery는 계속 동작한다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | invalid 또는 ambiguous due는 `invalid`로 저장되며 dispatch되지 않는다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | project/team WIP와 cost guard는 같은 resource lock 아래 admission을 거절한다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | stale/unknown quota는 0으로 바뀌지 않고, fresh high-confidence exhaustion만 worker hard gate가 된다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | retry budget 소진, worker blocked, required review는 열린 human escalation을 하나만 만든다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | 별도 Agent 승인 flag가 off이면 escalation resolve 요청은 `AMUX_AGENT_APPROVAL_UNAVAILABLE`로 거절된다. flag on 검증은 아래 G 구획을 별도 회차에서 수행하며, 이 구획만으로 승인 경로를 활성화하지 않는다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | task sync는 `doing`과 상태에 관계없이 이미 owner가 지정된 작업의 project/team identity를 바꾸지 않아 WIP slot을 우회하지 않는다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | Admin Routing 화면과 capture 어디에도 delivery prompt가 노출되지 않는다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | 캡처 프로세스의 deploy SHA 대조값이 true이고 기대한 전체 40자리 SHA와 같다. | pass | 첨부 capture의 `capture_process_revision_matches_expected=true`, `deployment_revision=68abcaa820303f3a2e05d644ff828f040dd39316` | - |
| | 자동 캡처의 `human_judgement.result`와 `signature`가 null이다. | pass | 첨부 capture의 `human_judgement.result=null`, `signature=null`; 사람 판정은 이 문서에만 기록 | - |
| | 관측 artifact digest와 stable state digest를 실행 기록에 옮겨 적고 파일을 immutable artifact로 보관한다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| **G. Agent 작업 검토 승인 (별도 flag-on 회차)** | | | | |
| | `TOMVERSE_AMUX_AGENT_APPROVAL_ENABLED=true`일 때만 보호 검토를 열 수 있고, `ops:write`·최근 재인증 없는 호출은 거절된다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | `AMUX_REVIEW_GITHUB_READ_TOKEN`은 `mposition/Tomverse`의 Pull requests와 Contents 읽기 전용이며, 값 자체는 화면·로그·기록에 나오지 않는다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | flag on에서 token·sync secret·`NEXTAUTH_URL` 중 하나가 빠지면 `/api/ready`의 `amuxReviewApproval`이 false이고, 모두 준비된 뒤에만 true다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | settle된 `review` 작업은 owner/claim이 해제되고, 이후 snapshot sync로 `review_pr_number`를 부여해도 해당 terminal attempt가 승인 대상이다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | `approve` 화면은 PR base SHA·head SHA·전체 diff와 그 SHA-256을 보이며, base/head/diff가 바뀐 이전 제안은 거절된다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | timeout·전송 실패 시 화면은 결과 미확인으로 잠기고, 발급된 결정 ID와 대상 digest로 조회해 commit 확인 전에는 다시 제출하지 않는다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | 이미 blocked인 task의 `block`은 불변 결정을 남기고 후속 escalation을 연다. 5회 소진은 기존 task를 재큐하지 않고 새 task 발행 계약을 따른다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | `retry`는 교정된 canonical due, 이전 attempt/delivery fence, 5회 상한, 현재 project/team 비용 상한을 확인한다. 다음 claim도 admission을 다시 한다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | 설명이 표시 한도를 넘으면 잘림을 보이고 `approve`·`retry`를 숨긴다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | 원문 조회·결정·거절의 Admin 감사 해시 체인을 확인하고, 원문·token이 원장·일반 GET·증거 파일에 저장되지 않았음을 확인한다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| **H. Rollback** | | | | |
| | `TOMVERSE_AMUX_EXECUTE`를 off로 되돌리면 새 execution이 시작되지 않는다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | `TOMVERSE_AMUX_EXECUTION_API_ENABLED`를 off로 되돌리면 execution mutation endpoint가 fail-closed다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | 필요 시 `TOMVERSE_AMUX_ENABLED`를 off로 내려 orchestrator 전체를 중단할 수 있다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | rollback은 기존 execution/delivery row를 수동 DB UPDATE/DELETE하지 않는다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | recovery는 canonical AMUX API/lease recovery 경로만 사용한다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |
| | production activation은 이 staging run의 별도 사람 판정과 승인 전에는 수행하지 않는다. | 미기록 | 이번 제한 회차에서 검증하지 않음 | - |

증거에는 다시 확인할 수 있는 로그 위치, request id, audit id, staging capture
위치 등을 적는다.

secret, token, task prompt, 사용자 데이터 자체는 넣지 않는다.

### 이번 제한 회차의 추가 관측

- Railway 검증 deployment `7cde1aff-2592-4b52-b854-c921b98fcb54`는 위 SHA로 `SUCCESS`였고, predeploy migration 로그에는 AMUX migration 네 개의 적용 성공이 남았다: `20260921120000_amux_attempt_budget`, `20260921123000_amux_incident_mode`, `20260921124000_amux_planning_resources`, `20260921160000_amux_agent_review_approval`.
- 자동 capture 생성 시각은 `2026-09-21T13:28:53.688Z`다. `evidence_scope=observed_facts_only`, incident는 `normal`·`valid=true`, 이 시점의 task·attempt·delivery·escalation 등 캡처 배열은 비어 있다. 이는 그 시점의 snapshot일 뿐 B–E의 동작 검증이나 해당 기간 전체의 무변경 증거가 아니다.
- 첨부 JSON의 SHA-256은 `6a3f0bbf0716cad0e2469d1513cca20c7f99feb2a1f4a6b0ba9f5fa10138ee54`; JSON 내부 `observation_digest_sha256`은 `454a86542cd1b317c5239cb9f22d7bebadf2f28fe4dd3a4f04e9b92abc4ffca2`, `stable_state_digest_sha256`은 `6c9d19d2ce29946e101e102d0f3e2bfc6a7bac87f410668369ccecf803ee1ff5`다. 검증 서비스에는 공개 도메인이 없어 Admin UI·routing API의 runtime 상태는 관측하지 않았다.
- 이 문서와 첨부 JSON은 동일한 로컬 Git commit에 보관한다. 원격 PR에 올리기 전까지 원격 보관은 확인되지 않았으므로, artifact 보관까지 요구하는 F 항목은 `미기록`이다.

자동 capture가 채우는 것은 관측과 digest뿐이다. `result`, activation 판정과 아래
서명은 실행자가 관측을 대조한 뒤 직접 결정한다.

## 판정

| 항목 | 값 |
|---|---|
| 결과 (통과 / 조건부 / 실패) | 통과 — 위에 명시한 source SHA·migration 적용·flag-off 설정·읽기 전용 자동 capture의 관측 범위에 한정 |
| activation 허용 여부 | 아니오. #6 flag-on/G 구획과 runtime 인증·Admin UI는 미기록이며 이 판정은 활성화 승인이 아님 |
| 조건부일 때의 조건 | 해당 없음. 범위 밖 검증은 별도 실행·판정이 필요 |
| 발견 사항 | 57개 항목 중 실제 확인한 4개만 `pass`; 나머지는 `미기록`. flag 값은 설정됐지만 runtime 응답을 관측하지 못함 |
| 후속 티켓 | 별도 staging flag-on 검증 회차가 필요. 이 기록은 PR 병합·production 배포 승인으로 사용하지 않음 |
| rollback 확인 | 미기록 — rollback 명령을 실행하지 않았고 H 구획은 별도 검증 대상 |

## 서명

| 항목 | 값 |
|---|---|
| 실행자 서명 | 없음 — 자동 관측·기록 전사는 Codex가 수행했으며 사람 실행자 서명은 제공되지 않음 |
| 실행 완료일 (ISO 8601) | 2026-09-21 (Railway 배포·캡처 완료일) |
| 승인자 서명 | mposition (사용자 제공 판정·서명) |
| 승인일 (ISO 8601) | 2026-09-22 |
