---
record: staging-verification
checklist: docs/ops/amux/staging-checklist.md
templateRevision: 2026-09-21
checklistSourceSha: 612b5c12684e038b36188805ca3821481bd73f00
runType: targeted-agent-review-flag-on
resultScope: protected-review-four-scenarios
deploymentId: 70c4bf42-1876-4b6a-a636-44c0616472bf
rollbackDeploymentId: 4980369f-17d2-41a4-9045-b92aba77483c
artifactDigest: sha256:98052a8a71dbf97bab254365a19ef5f07c56a30db20b7467fd0b313eea573107
environment: railway-staging-amux-validation-isolated-service
deploySha: 612b5c12684e038b36188805ca3821481bd73f00
startedAtUtc: 2026-09-23T11:28:37.987Z
completedAtUtc: 2026-09-23T12:11:06.146Z
executor: mposition (protected UI decisions); Codex (observation and transcription)
approver: mposition
approvedAtLocalDate: 2026-09-23
result: 통과
activationAllowed: false
frozen: true
digest: abf612e9d2924dbfff22f33d6d967753
stateDigest: e003262419d7f50bbd1e0d835e0c14b7166912ad21a9abc704ba15333433ff62
capturePath: docs/ops/amux/staging-verification-records/2026-09-23__612b5c12684e038b36188805ca3821481bd73f00.capture.json
---

# AMUX staging 검증 실행 — 2026-09-23 / 612b5c1

이 기록은 별도 Railway staging 검증 서비스에서 수행한 #6 보호 검토의 네 가지
시나리오에 한정한다. 실행하지 않은 항목은 `미기록`이며, 이 기록은 PR 병합,
production 배포 또는 외부 행위 승인이 아니다. fixture ID의 `124f08307cd7` 접두사는
공유 staging DB에 시료를 처음 만든 배포 SHA에서 유래하며, 실제 검토 화면과 자동
capture를 제공한 배포는 이 기록의 `612b5c12684e038b36188805ca3821481bd73f00`이다.
2026-09-23 mposition의 `통과` 판정은 이 명시된 범위와 공개된 발견 사항에만 적용된다.

## 실행 환경

| 항목 | 값 |
|---|---|
| 환경 | Railway `staging`의 격리 검증 서비스 `staging-amux-validation` |
| 배포 SHA (전체 40자리) | `612b5c12684e038b36188805ca3821481bd73f00` |
| deployment ID | `70c4bf42-1876-4b6a-a636-44c0616472bf` (`SUCCESS`) |
| template revision | 2026-09-21 |
| 시작 (UTC) | 2026-09-23T11:28:37.987Z (Railway deployment 생성) |
| 종료 (UTC) | 2026-09-23T12:11:06.146Z (최종 자동 capture 생성) |
| 실행자 | mposition (보호 UI 결정); Codex (관측·대조·기록 전사) |
| AMUX_ENABLED | Railway 서비스 변수 `0`; 활성화하지 않음 |
| AMUX_EXECUTE | Railway 서비스 변수 `0`; 활성화하지 않음 |
| AMUX_EXECUTION_API_ENABLED | Railway 서비스 변수 `0`; 활성화하지 않음 |
| AGENT_APPROVAL_ENABLED | Railway 서비스 변수 `true`; 이번 별도 flag-on 회차에만 사용 |
| worker catalog 상태 | 서비스 변수 목록에 없음 |
| executor config 상태 | 서비스 변수 목록에 없음 |
| execution_ready | 미기록 (Cloudflare Access와 local SSH 부재로 runtime readiness 응답 미수집) |

## 항목별 결과

각 항목은 `pass`, `fail`, `n/a`, `미기록` 중 하나를 사용한다. `pass`는 이 회차에서
직접 관측한 범위만 뜻한다.

| 구획 | 항목 | 결과 | 증거 | 후속 티켓 |
|---|---|---|---|---|
| **A. Source identity와 feature-off** | | | | |
| | staging이 보고하는 deploy SHA가 기록의 40자리 `deploySha`와 정확히 같다. | pass | Railway deployment source SHA, SHA-bound start command 및 capture `deployment_revision`이 모두 `612b5c12684e038b36188805ca3821481bd73f00`; match `true` | - |
| | `TOMVERSE_AMUX_ENABLED`가 unset 또는 `1` 이외 값인 상태를 관측한다. | pass | Railway 검증 서비스 변수 `0`; 이 회차 동안 변경하지 않음 | - |
| | `TOMVERSE_AMUX_EXECUTE`가 unset 또는 `1` 이외 값이다. | pass | Railway 검증 서비스 변수 `0`; execution delivery 0건 | - |
| | `TOMVERSE_AMUX_EXECUTION_API_ENABLED`가 unset 또는 `1` 이외 값이다. | pass | Railway 검증 서비스 변수 `0`; execution mutation을 활성화하지 않음 | - |
| | worker catalog와 executor command 설정이 activation 없이 주입되지 않았다. | pass | 검증 서비스 변수 이름 목록에 worker catalog/executor command 없음 | - |
| | server routing 응답의 lifecycle gate가 `execution_ready=false`다. | 미기록 | public request는 Cloudflare Access 302, 서비스 shell은 local SSH key 부재로 미관측 | AMUX-STG-FOLLOWUP |
| **B. 완전 비활성 상태** | | | | |
| | `TOMVERSE_AMUX_ENABLED`가 off일 때 orchestrator는 성공적으로 종료하며 internal URL 또는 AMUX sync secret을 요구하지 않는다. | 미기록 | 이번 회차는 보호 검토 flag-on 검증으로 orchestrator process를 별도 실행하지 않음 | - |
| | off 상태에서 worker runtime registration이 발생하지 않는다. | 미기록 | 기간 전체의 부재를 증명하는 전후 runtime capture를 만들지 않음 | - |
| | off 상태에서 task claim이 발생하지 않는다. | 미기록 | 동일 | - |
| | off 상태에서 execution attempt가 새로 시작되지 않는다. | 미기록 | 최종 capture는 fixture attempt만 보이나 기간 전체의 부재 증거는 아님 | - |
| | off 상태에서 durable delivery가 새로 생성되지 않는다. | 미기록 | 전후 capture 모두 `deliveries=[]`이고 다섯 결정 직후에도 0건이지만, 기간 전체를 연속 관측한 증거는 아님 | - |
| **C. Selection-only 상태** | | | | |
| | scheduler가 queue를 읽고 global-priority selection을 계산할 수 있다. | 미기록 | selection-only process 미실행 | - |
| | selection log가 선택과 실행을 분리해 표시한다. | 미기록 | selection log 미수집 | - |
| | selection-only 동안 task owner/revision이 selection 자체 때문에 변하지 않는다. | 미기록 | selection-only process 미실행 | - |
| | execution attempt가 생성되지 않는다. | 미기록 | selection-only process 미실행 | - |
| | durable delivery가 생성되지 않는다. | 미기록 | selection-only process 미실행 | - |
| | executor configuration이 없어도 selection-only service가 실행된다. | 미기록 | selection-only process 미실행 | - |
| **D. Fencing과 durable execution contract** | | | | |
| | worker runtime identity가 instance id + generation으로 fenced된다. | 미기록 | 이번 제한 회차에서 미실행 | - |
| | stale/replacement generation은 heartbeat/settlement 권한을 잃는다. | 미기록 | 이번 제한 회차에서 미실행 | - |
| | execution start와 durable delivery가 하나의 atomic commit이다. | 미기록 | 이번 제한 회차에서 미실행 | - |
| | live delivery receipt retry는 같은 receipt를 유지한다. | 미기록 | 이번 제한 회차에서 미실행 | - |
| | receipt lease 만료 뒤 pull은 새 receipt를 발급한다. | 미기록 | 이번 제한 회차에서 미실행 | - |
| | 만료되거나 교체된 receipt의 ACK는 거절된다. | 미기록 | 이번 제한 회차에서 미실행 | - |
| | worker fence loss 뒤 executor가 취소되고 stale settlement가 전송되지 않는다. | 미기록 | 이번 제한 회차에서 미실행 | - |
| | fenced/ambiguous worker는 같은 process에서 자동 재등록되지 않는다. | 미기록 | 이번 제한 회차에서 미실행 | - |
| **E. Audit와 recovery** | | | | |
| | claim decision에는 scheduler와 worker-router evidence가 함께 남는다. | 미기록 | claim을 실행하지 않음 | - |
| | execution start/settle/recovery가 canonical admin audit chain에 연결된다. | 미기록 | fixture의 과거 settle은 합성 시료이며 실제 execution start 회차가 아님 | - |
| | expired current execution recovery만 Todo로 반환되고 stale attempt는 변경하지 못한다. | 미기록 | 이번 제한 회차에서 미실행 | - |
| | expired owner reservation recovery가 기존 routing evidence를 삭제하지 않는다. | 미기록 | 이번 제한 회차에서 미실행 | - |
| | audit integrity verification이 staging evidence 범위에서 정상이다. | fail | 전역 검사는 185건 중 176건 검증, historical invalid 9건, linkage break 0건. 이번 #6 decision audit 5건은 각각 저장 해시가 일치함 | AMUX-STG-AUDIT-HISTORY |
| **F. Advanced planning guard와 자동 증거** | | | | |
| | incident freeze 중 새 claim/start는 거절되고, freeze 전에 만들어진 durable delivery와 heartbeat/settle/recovery는 계속 동작한다. | 미기록 | incident capture는 `normal`; freeze 전환 미실행 | - |
| | invalid 또는 ambiguous due는 `invalid`로 저장되며 dispatch되지 않는다. | 미기록 | 이번 시료는 valid/none due만 사용 | - |
| | project/team WIP와 cost guard는 같은 resource lock 아래 admission을 거절한다. | 미기록 | 이번 시료에는 project/team resource policy가 없음 | AMUX-STG-COST-ADMISSION |
| | stale/unknown quota는 0으로 바뀌지 않고, fresh high-confidence exhaustion만 worker hard gate가 된다. | 미기록 | quota telemetry 시료 없음 | - |
| | retry budget 소진, worker blocked, required review는 열린 human escalation을 하나만 만든다. | 미기록 | 최종 snapshot에서 task별 열린 escalation 중복은 없지만 최초 4건은 fixture가 직접 열어 자연 발생 경로 전체를 검증하지 않음 | - |
| | 별도 Agent 승인 flag가 off이면 escalation resolve 요청은 `AMUX_AGENT_APPROVAL_UNAVAILABLE`로 거절된다. flag on 검증은 아래 G 구획을 별도 회차에서 수행하며, 이 구획만으로 승인 경로를 활성화하지 않는다. | 미기록 | 이번 회차는 flag `true`; off 경로는 staging에서 재실행하지 않음 | - |
| | task sync는 `doing`과 상태에 관계없이 이미 owner가 지정된 작업의 project/team identity를 바꾸지 않아 WIP slot을 우회하지 않는다. | 미기록 | task sync 미실행 | - |
| | Admin Routing 화면과 capture 어디에도 delivery prompt가 노출되지 않는다. | pass | 사용자가 관측한 네 보호 검토 화면과 최종 capture에 delivery prompt/본문 없음 | - |
| | 캡처 프로세스의 deploy SHA 대조값이 true이고 기대한 전체 40자리 SHA와 같다. | pass | capture `capture_process_revision_matches_expected=true` | - |
| | 자동 캡처의 `human_judgement.result`와 `signature`가 null이다. | pass | capture 두 값 모두 null; 자동화가 사람 판정을 쓰지 않음 | - |
| | 관측 artifact digest와 stable state digest를 실행 기록에 옮겨 적고 파일을 immutable artifact로 보관한다. | 미기록 | digest는 front matter에 전사했으나 아직 commit되지 않아 immutable 보관 완료 전 | - |
| **G. Agent 작업 검토 승인 (별도 flag-on 회차)** | | | | |
| | `TOMVERSE_AMUX_AGENT_APPROVAL_ENABLED=true`일 때만 보호 검토를 열 수 있고, `ops:write`·최근 재인증 없는 호출은 거절된다. | 미기록 | flag-on, 인증된 Admin의 성공 경로는 관측했으나 권한/재인증 음성 경로를 staging에서 별도 실행하지 않음 | - |
| | `AMUX_REVIEW_GITHUB_READ_TOKEN`은 `mposition/Tomverse`의 Pull requests와 Contents 읽기 전용이며, 값 자체는 화면·로그·기록에 나오지 않는다. | 미기록 | token 존재와 비노출은 확인; 발급 화면의 scope를 독립 재확인하지 못함 | AMUX-STG-TOKEN-SCOPE |
| | flag on에서 token·sync secret·`NEXTAUTH_URL` 중 하나가 빠지면 `/api/ready`의 `amuxReviewApproval`이 false이고, 모두 준비된 뒤에만 true다. | 미기록 | 값 존재는 확인했으나 missing-variable readiness permutation 미실행 | - |
| | private review origin을 쓰는 경우 현재 app service의 loopback 또는 `RAILWAY_PRIVATE_DOMAIN` 및 실제 `PORT`와 정확히 결속되고, 다른 port·internal/public host는 readiness와 proxy 양쪽에서 거절된다. | 미기록 | `http://127.0.0.1:8080`과 `PORT=8080`의 성공 및 실제 review detail 200은 관측; 거절 경로와 readiness는 live 미실행 | - |
| | 결정 mutation 전에 protected review detail을 한 번 열어 private origin의 실제 도달성을 확인한다. readiness는 형식 결속만 증명하며 네트워크 도달성을 대신하지 않는다. | pass | 네 결정 모두 상세 화면에서 task/revision/escalation/digest를 확인한 뒤 proposal 생성 | - |
| | settle된 `review` 작업은 owner/claim이 해제되고, 이후 snapshot sync로 `review_pr_number`를 부여해도 해당 terminal attempt가 승인 대상이다. | 미기록 | 합성 attempt와 생성 시점의 `review_pr_number`로 owner/claim 해제 후 승인은 관측했으나 실제 settle 및 후속 snapshot sync 경로는 미실행 | - |
| | `approve` 화면은 PR base SHA·head SHA·전체 diff와 그 SHA-256을 보이며, base/head/diff가 바뀐 이전 제안은 거절된다. | 미기록 | 표시·성공은 관측: PR #1594; base `96a6990ee82cfa624affefac43e711a5244cd9ae`, head `612b5c12684e038b36188805ca3821481bd73f00`, diff SHA-256 `c352af3a73eb3ba0a779f47efa40ce9b681bf2695c81918ce64378ee485028bd`; 이동 후 거절은 live 미실행 | - |
| | timeout·전송 실패 시 화면은 결과 미확인으로 잠기고, 발급된 결정 ID와 대상 digest로 조회해 commit 확인 전에는 다시 제출하지 않는다. | 미기록 | 정상 성공 경로만 실행; timeout/전송 실패를 staging에서 주입하지 않음 | AMUX-STG-UNKNOWN-RESULT |
| | 이미 blocked인 task의 `block`은 불변 결정을 남기고 후속 escalation을 연다. 5회 소진은 기존 task를 재큐하지 않고 새 task 발행 계약을 따른다. | 미기록 | blocked block 뒤 새 escalation과 retry, exhausted task의 재큐·새 escalation 없음은 관측. 소진 뒤 별도 새 task 발행은 실행하지 않음 | - |
| | `retry`는 교정된 canonical due, 이전 attempt/delivery fence, 5회 상한, 현재 project/team 비용 상한을 확인한다. 다음 claim도 admission을 다시 한다. | 미기록 | decision `2df57966-64b7-433b-bd51-2837d9fd7a13`: due valid, attempt 1/5 유지, owner/claim 해제를 확인. 시료에 project/team 비용 정책이 없어 비용 상한은 미검증 | AMUX-STG-COST-ADMISSION |
| | 설명이 표시 한도를 넘으면 잘림을 보이고 `approve`·`retry`를 숨긴다. | pass | 70,000자 fixture에서 잘림 경고와 scroll을 표시하고 `Block`만 허용; decision `935aae33-41b5-4800-abd0-126440dbac38` | - |
| | 원문 조회·결정·거절의 Admin 감사 해시 체인을 확인하고, 원문·token이 원장·일반 GET·증거 파일에 저장되지 않았음을 확인한다. | 미기록 | 다섯 decision audit의 저장 해시는 개별 일치하고 capture/일반 화면에 원문·token 없음. 거절 audit 미실행, 전역 원장에 historical invalid 9건 존재 | AMUX-STG-AUDIT-HISTORY |
| **H. Rollback** | | | | |
| | `TOMVERSE_AMUX_EXECUTE`를 off로 되돌리면 새 execution이 시작되지 않는다. | n/a | 이 회차 시작부터 끝까지 `0`; on으로 올리지 않음 | - |
| | `TOMVERSE_AMUX_EXECUTION_API_ENABLED`를 off로 되돌리면 execution mutation endpoint가 fail-closed다. | n/a | 이 회차 시작부터 끝까지 `0`; on으로 올리지 않음 | - |
| | 필요 시 `TOMVERSE_AMUX_ENABLED`를 off로 내려 orchestrator 전체를 중단할 수 있다. | n/a | 이 회차 시작부터 끝까지 `0` | - |
| | rollback은 기존 execution/delivery row를 수동 DB UPDATE/DELETE하지 않는다. | pass | rollback·수동 DB UPDATE/DELETE를 실행하지 않음; 결정을 canonical API로만 수행 | - |
| | recovery는 canonical AMUX API/lease recovery 경로만 사용한다. | n/a | recovery 미실행 | - |
| | production activation은 이 staging run의 별도 사람 판정과 승인 전에는 수행하지 않는다. | pass | production 설정·배포·병합 변경 없음; `activationAllowed=false` | - |

## 시나리오 결과 요약

| 시나리오 | 결과 | 불변 결정 / 현재 상태 |
|---|---|---|
| PR에 결속된 `review → done` | 관측 완료(부분) | UI 결정과 최종 상태는 관측: decision `0dd4127d-7b00-4267-8298-5207b17ee56e`, task `done` r3. 실제 settle·후속 snapshot sync 경로는 미실행 |
| 이미 blocked인 작업 `block`, 이후 `retry` | 관측 완료(부분) | block `04f23777-9456-4703-8fb1-d577785c842b`, retry `2df57966-64b7-433b-bd51-2837d9fd7a13`; task `todo` r4, attempt 1/5 유지. 실제 resource cost policy는 없음 |
| 5회 소진 작업 | 관측 완료(부분) | decision `11e371cf-353c-4aaa-b889-50a4c77a76f9`; task `blocked` r7, 5/5 유지, 재큐·새 escalation 없음. 별도 새 task 발행은 미실행 |
| oversized review content | 관측 완료 | decision `935aae33-41b5-4800-abd0-126440dbac38`; task `blocked` r3. source가 여전히 oversized라 새 escalation에서도 `block`만 가능 |

결정 전 baseline capture는
`2026-09-22__124f08307cd74700ecee1016133b3ac766da247c.capture.json`이며 SHA-256은
`3bdad2261b46995f27bf1de31d2f17bafa83c91352146d6d7b1245c68bab8aea`다. 최종
capture에는 `done/none` 1건, `todo/valid` 1건, `blocked/valid` 1건,
`blocked/none` 1건이 있고, delivery·route decision·resource policy·quota·cost ledger는
없다. capture 파일 SHA-256은
`98052a8a71dbf97bab254365a19ef5f07c56a30db20b7467fd0b313eea573107`, 내부
observation digest는
`99354616538907550baaae5921d64613f5658739fc451c0abb6615fefd24fe39`, stable state
digest는 `e003262419d7f50bbd1e0d835e0c14b7166912ad21a9abc704ba15333433ff62`다.

## 판정

| 항목 | 값 |
|---|---|
| 결과 (통과 / 조건부 / 실패) | 통과 — 보호 검토 네 시나리오의 명시된 관측 범위에 한정. 실행하지 않은 결합 조건과 전역 audit historical invalid 9건은 별도 공개 |
| activation 허용 여부 | 아니오. 이 판정은 병합·production·외부 행위 승인이 아님 |
| 조건부일 때의 조건 | 해당 없음. 범위 밖인 `timeout/result unknown`, 실제 project/team cost admission, token scope, readiness 음성 경로 및 historical audit 9건은 별도 후속 작업 |
| 발견 사항 | 초기 fixture escalation 4건은 `openedBy=system:staging-review-fixture`이나 재사용한 writer 때문에 audit `systemActor=tomverse-amux-orchestrator`로 남았다. orchestrator는 실행되지 않았고 이 행은 삭제·수정하지 않는다. 해당 생성기는 commit 대상에서 제거해 재사용을 막았다. oversized source가 교정되지 않으면 후속 escalation도 `block`만 제공하므로 source 단축 후 다시 검토해야 한다. #6의 다섯 decision audit는 개별 해시 일치, 원장 linkage break 0건 |
| 후속 티켓 | `AMUX-STG-UNKNOWN-RESULT`, `AMUX-STG-COST-ADMISSION`, `AMUX-STG-TOKEN-SCOPE`, `AMUX-STG-AUDIT-HISTORY` |
| rollback 확인 | execution 관련 세 flag는 계속 off. 별도 검증 서비스의 approval flag는 `false`, 임시 이메일 한도는 코드 기본값 `3/12`로 원복했고 deployment `4980369f-17d2-41a4-9045-b92aba77483c`가 `SUCCESS`. production 변경 없음 |

## 서명

| 항목 | 값 |
|---|---|
| 실행자 서명 | mposition (보호 UI 결정); Codex (관측·기록 전사) |
| 실행 완료일 (ISO 8601) | 2026-09-23 |
| 승인자 서명 | mposition |
| 승인일 (ISO 8601) | 2026-09-23 |
