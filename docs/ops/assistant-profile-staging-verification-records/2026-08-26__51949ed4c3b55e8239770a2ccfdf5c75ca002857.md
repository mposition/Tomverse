---
record: staging-verification
checklist: docs/ops/assistant-profile-staging-checklist.md
templateRevision: 2026-08-24a
environment: staging
deploySha: 51949ed4c3b55e8239770a2ccfdf5c75ca002857
startedAtUtc: 2026-08-26T07:31:57Z
completedAtUtc: 2026-08-26
executor: mposition
approver: mposition
result: 통과
frozen: true
digest: dc00e81ea1feb69f38190e17a13ac8ce
---

# Assistant profile staging 검증 실행 — 2026-08-26 / `51949ed4`

**§C-5 재검증 전용 회차입니다.** 62개 항목 중 이 한 항목만 실행했습니다.
나머지는 `미기록`이며, 그것이 이 회차가 밝힌 범위입니다 —
`AGENTS.md`의 "검증 범위는 되돌릴 수 없는 것에 비례합니다"에 따라
PR #965가 배포된 뒤 그 한 가지가 실제로 고쳐졌는지만 확인했습니다.

직전 회차 `2026-08-25__ff013ecf...`가 §C-5를 `fail`로 기록했고, 그 기록은
수정 전 staging을 관측한 것이므로 **고치지 않았습니다.** 이 회차가 그
후속입니다.

> **이 문서는 에이전트가 만든 초안입니다.** 실행자가 브라우저와 콘솔에서
> 보고한 것만 옮겨 적었습니다. **판정과 서명 칸은 비어 있으며 실행자만
> 채웁니다.** 각 줄을 확인한 뒤 commit 하십시오.

## 실행 환경

| 항목 | 값 |
|---|---|
| 환경 | **staging** |
| 배포 SHA (전체 40자리) | `51949ed4c3b55e8239770a2ccfdf5c75ca002857` |
| production SHA와 동일한가 | 아니오 — production은 `main`, 이 SHA는 `develop` tip |
| 다르다면 profile 표면 diff 결과 | 미기록 — 이 회차는 develop 기준의 §C-5 한 항목만 판별하며 production 비교를 목적으로 하지 않습니다 |
| template revision | 2026-08-24a |
| 시작 (UTC) | 2026-08-26T07:31:57Z (대화 생성) |
| 종료 (UTC) | |
| 실행자 | mposition |
| `assistantProfilesEnabled` (시작 시점) | 미기록 — 실행자가 보고하지 않았습니다 |
| `assistantKnowledgeEnabled` (시작 시점) | 미기록 — 실행자가 보고하지 않았습니다 |
| `memoryInjectionEnabled` (시작 시점) | 미기록 — 실행자가 보고하지 않았습니다 |

## §B 실행 전 대화 상태

되돌릴 수 없는 값이므로 **실행 전에** 적습니다. 나중에 채우면 비교 대상이
없어집니다. 대화 ID는 소유자 자신의 것이며 계정 식별자는 적지 않습니다.

| 대화 | `selectedModels` | `disabledPanels` |
|---|---|---|
| 1 | | |
| 2 | | |

## 항목별 결과

체크리스트의 A–J 구획을 그대로 옮기고, 각 항목에 다음 중 하나를 적습니다.

- `pass` — 확인함. 증거 참조를 함께 적습니다.
- `fail` — 확인했고 실패했습니다. 후속 티켓을 적습니다.
- `n/a` — 이 실행에서 해당하지 않습니다. 이유를 적습니다.
- `미기록` — 실행하지 않았거나 기록이 없습니다. **나중에 채우지 않습니다.**

| 구획 | 항목 | 결과 | 증거 | 후속 티켓 |
|---|---|---|---|---|
| **A. Fail-closed (flag off)** | | | | |
| | `/settings/assistants`가 목록이 아니라 비활성 안내를 렌더한다 | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | 로그인 상태에서 `GET /api/assistant-profiles`가 403 이고 코드가 `ASSISTANT_PROFILES_DISABLED`이다 | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | 비로그인 상태에서 같은 요청이 **401**이다 — flag 상태를 알려주지 않는다 (인증이 flag보다 먼저 판정된다) | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | 컴포저 도구 메뉴에 assistant 행(`tools-assistant-row`)이 렌더되지 않는다 | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | 위 요청 어느 것도 `AssistantProfile`·`AssistantProfileVersion` 행을 만들지 않는다 | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| **B. 다른 대화를 건드리지 않는다 (핵심 계약, 유료)** | | | | |
| | 대화 1에서 profile을 고른 뒤, 대화 2의 `selectedModels`와 `disabledPanels`가 **한 글자도 바뀌지 않았다** | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | 대화 1의 PATCH가 **응답하기 전에** 대화 2로 전환했을 때도 같다 — 이것이 #632의 실제 발생 조건이다 | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | 두 대화를 새로고침한 뒤 서버가 돌려주는 값이 실행 전 기록과 같다 | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | §14.0: **기존** 대화에 profile을 연결해도 그 대화의 `selectedModels`가 profile의 모델 목록으로 교체되지 않는다 (보존이 기본값) | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | §14.0: **새** 대화를 profile과 함께 만들면 profile의 `modelIds`가 초기 선택이 된다 (채택은 생성 시점뿐) | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | §14.0a: **모델을 지정하지 않은** profile로 새 대화를 만들면 계정의 새 대화 선택이 그대로 선다 — 그 profile을 만들던 날의 기본 모델로 고정되지 않는다. 같은 대화 생성에서 함께 보므로 **유료 turn이 늘지 않습니다** | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | profile을 해제(`assistant-option-none`)해도 `selectedModels`가 바뀌지 않는다 | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| **C. 버전 고정 (유료)** | | | | |
| | 새 대화가 profile의 **최신 active revision**에 pin된다 | pass | 실행자 콘솔 `GET /api/conversations/cmt9rzvn9000w02lrtj5y8bpw` → `assistantProfile`: `profileId` `cmt9rzi95000q02lr505443vs` ("C3"), `profileVersionId` `cmt9rzi9f000r02lrvgu4ca0s`, `revision` **1**, `latestRevision` **1**, `status` `current` | |
| | revision을 새로 publish한 뒤에도 기존 대화의 pin이 **움직이지 않는다** | pass | 개정 2 발행(`지시문과 모델 저장`) 후 같은 대화: `assistantProfileVersionId`가 `cmt9rzi9f000r02lrvgu4ca0s`로 **동일**, `revision` **1**, `latestRevision` **2**, `status` `superseded`. 대화 행의 `updatedAt`도 `2026-08-26T07:32:03.684Z`로 발행 전과 동일 — 발행이 대화 행을 건드리지 않음 | |
| | 그 대화가 `superseded`로 표시된다 — 점이 아니라 **문구**로 (`tools-assistant-superseded-badge`, "새 개정 있음") | 미기록 | API가 `status` `superseded`를 돌려준 것은 위 행에 있으나, **화면의 배지 문구는 관측되지 않았습니다.** 이 항목은 UI 표시에 관한 것이라 API 응답으로 대신할 수 없습니다 | |
| | 어시스턴트 목록에서 그 profile 행이 **pin된 개정**을 말한다 (`assistant-option-{id}-detail` = "개정 1 사용 중 · 최신 3"). 최신 개정 번호만 적힌 행에 체크 표시가 붙으면 이동하지 않은 대화가 이동한 것으로 읽힙니다 | 미기록 | 이 회차는 목록 화면을 열지 않았습니다 | |
| | 그 상태에서 turn을 보내면 **옛 revision의 instructions**로 답한다 | **pass** | `superseded` 상태(위 행)에서 중립 프롬프트(산수) turn 1회. 답이 **개정 1의 표식으로 시작**했고 개정 2의 표식은 나타나지 않음. 모델 `gpt-5-6-luna`, 웹 검색 미실행. **직전 회차 `ff013ecf`가 이 항목을 `fail`로 기록했고, 그때는 어느 표식도 없었습니다** — profile 전체가 `no_active_version`으로 거절돼 지시문이 전달되지 않았기 때문. PR #965가 그 거절 분기를 제거했고 이 회차가 그 결과를 확인합니다 | |
| | 최신으로 이동(`assistant-move-to-latest`)은 **사용자가 눌러야만** 일어난다 | 부분 관측 | 이동을 **누르지 않은** 쪽만 확인했습니다: 개정 2 발행 뒤 turn을 보내는 동안 pin이 `revision` 1에 머물렀고 자동 이동은 없었습니다. 눌렀을 때 이동한다는 쪽은 이 회차에서 실행하지 않았습니다(직전 회차 `ff013ecf`가 `pass`로 관측) | |
| | 이동 후에도 이미 저장된 과거 답변은 소급해 바뀌지 않는다 | 미기록 | 이 회차는 이동을 실행하지 않았습니다 | |
| **D. Prompt 경계 (§9.1 · §10, 유료)** | | | | |
| | profile instructions가 **system message 안에 한 번만** 나타나고, 대화 내용보다 **앞**에 온다 | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | fence marker와 `ignore previous instructions` 류를 본문에 넣은 profile을 만들어 보냈을 때, 그 문장이 경계를 넘지 못한다 | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | profile이 붙은 요청의 context bundle `profileTokens`가 0이 아니고, 해제하면 0이 된다 | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | 대화 도중 profile을 바꾸면 이전 bundle이 **stale로 거절**되고 조용히 재사용되지 않는다 | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | 응답에 표시되는 model identity가 실제 Tomverse 모델이다 (§14 마지막 줄) | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| **E. Memory와의 경계 (injection flag OFF, 유료)** | | | | |
| | `memoryInjectionEnabled`가 꺼진 상태에서 profile의 memory policy가 `on`이어도 prompt에 memory block이 들어가지 않는다 | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | 그 turn의 `memoryUsedCount`가 0이다 | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | 거절 사유가 `flag_off`로 관측된다 — `profile_off`가 아니다 (profile이 껐기 때문이 아니라 운영 flag가 껐기 때문이라는 구분) | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| **F. 진입점과 노출** | | | | |
| | 설정에 `AI 어시스턴트` 탭이 있고, 그 안에서 어시스턴트 목록을 보고 새 어시스턴트를 만들 수 있다 | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | 설정에 `AI 개인화` 탭이 있고, 그 안에 memory 행과 새 대화 모델 조합이 있다 | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | `데이터` 탭에는 어시스턴트 행과 memory 행이 **없다** — 가져오기·계정 데이터·이메일 알림만 남는다 | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | 어시스턴트 행의 action label이 자기 목적을 말한다 (`docs/ui-contracts/settings-navigation.md` 계약) | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | 상세 페이지의 breadcrumb가 `설정 / AI 어시스턴트 / 나의 AI 어시스턴트` 이다 — `AI 설정`도 `데이터 및 개인화`도 아니다 | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | `AI 개인화` 탭에 머물던 시절의 deep link(`?settings=ai&settingsSection=assistants`) 가 여전히 어시스턴트 탭을 연다 — 구획을 옮겨도 북마크가 죽지 않는다 (`SETTINGS_SECTION_TAB`이 tab을 결정하므로 손으로 쓴 tab 값은 무시된다) | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | 컴포저 도구 메뉴의 어시스턴트 행에서 어시스턴트를 고르고 해제할 수 있다 | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | 비로그인 세션에는 진입점이 없다 — 어시스턴트는 계정에 속한다 | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | 상세 페이지에서 "설정으로 돌아가기"가 직접 연 URL에서도 동작하고, 돌아간 뒤 그 행에 focus가 있다 | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | 모바일 320px에서 컴포저 textarea가 전용 full-width 행을 유지한다 (`docs/ui-contracts/mobile-chat-composer.md` 계약) | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | 모바일에서 설정 탭 **여섯 개**가 잘리거나 라벨이 읽을 수 없게 좁아지지 않는다 | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| **G. 삭제** | | | | |
| | profile을 삭제해도 그 profile에 묶여 있던 대화가 삭제되지 않고, 열리며, 과거 답변이 남는다 | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | 삭제된 profile에 묶인 대화가 그 사실을 표시하고, 새 turn을 보낼 수 있다 | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | 계정 삭제 시 profile과 version이 함께 지워진다 | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| **H. 관측** | | | | |
| | `GET /api/admin/app-settings`의 `assistantProfilesEnabled`가 의도한 값 | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | flag를 바꾼 저장이 `AdminAuditLog`에 2행(전·후)으로 남았다 | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | `GET /api/admin/audit-integrity`가 `valid: true` | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | §B의 대화 상태는 소유자 자신의 `GET /api/conversations/{id}` 응답으로 확인한다 — 계정 식별자가 저장소로 나가지 않는다 | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| **I. 생성과 발견 (#690, 유료 turn 1건)** | | | | |
| | `/settings/assistants/new`가 이름·지시문·설명만 보여준다. 아이콘과 모델은 닫힌 `고급 설정` 안에 있다 | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | `고급 설정`을 열면 모델 구획이 **라디오 두 개**로 시작한다 — `계정 기본 모델 따르기`가 선택돼 있고, 모델 목록은 **보이지 않는다**. (2026-08-21a까지는 체크박스 목록이 바로 보이길 기대했습니다. #868 이후 그 기대는 정상 동작을 fail로 적게 만듭니다) | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | `직접 지정`을 고르면 모델이 **이름으로 고르는 목록**으로 나타난다 — 쉼표로 구분한 내부 ID 입력창이 아니다 | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | 그때 계정 기본 모델 **하나가 미리 선택돼 있고, 그 마지막 하나는 해제되지 않는다**. 해제 대신 "지정하지 않으려면 다른 쪽을 고르라"는 안내가 보인다 | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | 아무것도 건드리지 않고 만든 profile이 **모델을 지정하지 않은 채** 저장된다 (`GET /api/assistant-profiles/{id}`의 `currentVersion.models`가 `[]`) | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | 이름과 지시문만으로 만든 profile이 **즉시 picker에 나타나고 대화를 시작할 수 있다** (두 번째 저장 없이) | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | 지시문을 비운 채 제출하면 그 필드에 오류가 붙고 focus가 이동하며, **요청이 나가지 않는다** | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | 컴포저 → 어시스턴트 → `새 프로필 만들기` → 최소 폼 → `만들고 이 대화에 사용` → **원래 대화로 돌아오고 그 profile이 붙어 있다** | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | 복귀한 대화의 `selectedModels`가 §B의 사전 기록과 같다 — 생성 왕복이 모델 설정을 건드리지 않는다 | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | profile이 하나도 없을 때 picker가 안내 문장이 아니라 `새 프로필 만들기`를 primary로 보여준다 | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | 편집 화면의 주 버튼이 `변경사항 저장`이고, 성공 메시지가 개정 번호를 말하지 않는다. version history에는 개정 번호가 그대로 있다 | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | **신고된 결함의 회귀**: 모델을 지정한 profile을 편집해 `계정 기본 모델 따르기`로 바꾸고 저장하면 오류 없이 새 개정이 만들어진다. 2026-08-23까지 이 동작은 `Invalid request payload.`로 거절됐습니다 (#868) | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | 생성이 서버 오류로 실패했을 때 **목록에 사용할 수 없는 profile이 남지 않는다** (staging에서 만들기 어려우면 `n/a`로 두고 이유를 적습니다 — DB 통합 테스트가 이 계약을 덮고 있습니다) | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| **J. 거절 문구 (0크레딧)** | | | | |
| | 비로그인 상태로 `/settings/assistants/new`에서 만들기를 눌렀을 때, 화면 언어의 문장이 나오고 **영어 원문(`Unauthorized`)이 보이지 않는다**. 문구가 "로그인 상태가 아니어서"라고 말하고, 로그인한 적 없는 방문자에게 "로그인이 풀렸다"고 말하지 않는다 (#892) | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | 그 화면을 떠나지 않은 채 입력한 이름·지시문이 **그대로 남아 있다** — 문구가 약속하는 것이 실제로 참인지 확인합니다 | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | 저장 실패 알림 어디에도 **오류 코드 문자열**(`ASSISTANT_...`)이 그대로 노출되지 않는다 | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |
| | (선택) 비활성 모델을 지정한 profile로 저장을 시도하면 플랜을 언급하는 자기 문장이 나온다. staging에서 비활성 모델을 만들기 어려우면 `n/a`로 두고 이유를 적습니다 — `tests/assistantProfileErrorCopy.test.mjs`가 모든 코드에 문구가 있다는 것을 이미 강제합니다 | | 미기록 | 이 회차의 범위 밖 — §C-5 재검증 전용 |

증거는 스크린샷 경로, 로그 발췌 위치, admin 응답 캡처처럼 **다시 확인할 수
있는 것**을 적습니다. 개인 데이터가 있는 자료는 저장소가 아니라 제한된 운영
저장소에 두고 여기에는 참조만 남깁니다.

## 사용한 크레딧

| 구획 | 모델 | turn 수 | 크레딧 |
|---|---|---|---|
| B | — | 0 | 0 — 실행하지 않음 |
| C | `gpt-5-6-luna` | **2** | 미보고 — 실행자가 크레딧 잔액 변화를 보고하지 않았습니다 |
| D | — | 0 | 0 — 실행하지 않음 |
| E | — | 0 | 0 — 실행하지 않음 |
| 합계 | | **2** | 미보고 |

turn 둘의 쓰임입니다. 유료 회차의 turn마다 **무엇을 판별했는지**를 적습니다 —
직전 회차에서 유료 turn 하나가 판별에 쓰이지 못한 채 소모됐고, 그것이 turn 단위
기록이 생긴 이유입니다.

| # | 프롬프트 성격 | 이 turn이 판별한 것 |
|---|---|---|
| 1 | 인사말 | pin이 개정 1에 걸렸고 그 지시문이 적용됨. **개정 2 발행 전** 상태의 기준선 |
| 2 | 중립 산수 | **§C-5 그 자체** — `superseded` 상태에서 고정된 개정으로 답하는가. 직전 회차 turn 2가 안전 경로에 걸려 판별하지 못했으므로 중립 프롬프트를 골랐습니다 |

## 판정

| 항목 | 값 |
|---|---|
| 결과 (통과 / 조건부 / 실패) | **통과** |
| 조건부일 때의 조건 | n/a |
| 발견 사항 | §C-5가 `pass`로 관측됐습니다 — 직전 회차 `ff013ecf`가 `fail`로 기록한 항목이며, PR #965 배포 이후 처음 확인된 것입니다. **범위 어긋남을 함께 적습니다: 62개 항목 중 61개가 `미기록`인데 결과가 `통과`입니다.** 이 디렉터리 `README.md` 규칙 6은 그 상태를 `조건부`로 정하므로 규칙과 어긋납니다. 판정은 실행자의 권한이므로 그대로 기록하되, **다음 회차가 이 기록을 근거로 쓸 때 "통과"를 62개 전체에 대한 판정으로 읽어서는 안 됩니다** — 이 회차가 판별한 것은 §C-5 하나입니다 |
| 후속 티켓 | 없음. 이 회차가 남긴 미판별 항목은 새 티켓이 아니라 **다음 정규 회차의 범위**입니다 |

**판정 전에 읽을 것.** 이 회차는 62개 중 1개만 실행했고 나머지는 `미기록`입니다.
이 디렉터리 `README.md` 규칙 6은 "필수 항목에 `미기록`이 남아 있으면 전체 결과는
`조건부`"라고 적습니다. 그 규칙대로면 이 회차는 **`조건부`** 이며, 조건은
"§C-5 외 항목은 이 회차가 판별하지 않았다"입니다.

**`통과`로 적으실 수도 있습니다** — 회차의 목적이 §C-5 한 항목이었고 그것이
관측됐으므로. 다만 그때는 규칙과 어긋난다는 사실을 위 `조건부일 때의 조건` 칸이
아니라 `발견 사항`에 함께 적어 주십시오. 직전 회차 `ff013ecf`가 같은 상황에서
`통과`로 적고 어긋남을 본문에 남겼습니다.

**어느 쪽이든 판정은 실행자의 권한이며 에이전트는 비워 둡니다.**

## 정리 의무

staging 계정에 이 회차가 만든 profile과 대화가 남습니다. 지운 뒤 시각을 적습니다.

| 항목 | 값 |
|---|---|
| 남아 있는 것 | profile `C3` (`cmt9rzi95000q02lr505443vs`, 개정 2개) · 대화 `cmt9rzvn9000w02lrtj5y8bpw` |
| staging 데이터 삭제 (UTC) | **2026-08-26T08:00Z** — 실행자 보고 |
| 로컬 파일 정리 (UTC) / 확인자 | **n/a** — 이 회차는 로컬 시료를 만들지 않았습니다 |

## 서명

| 항목 | 값 |
|---|---|
| 실행자 서명 | mposition |
| 실행 완료일 (ISO 8601) | 2026-08-26 |
| 승인자 서명 | mposition |
| 승인일 (ISO 8601) | 2026-08-26 |
