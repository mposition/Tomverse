---
record: staging-verification
checklist: docs/ops/assistant-profile-staging-checklist.md
templateRevision: 2026-08-21a
runType: production_activation
environment: production
deploySha: 92148e6da91be6bcd761e33115bb8e71085f4b62
deploymentId: 83cdf044-f447-4fa1-9346-44bbe3d9cb22
appliedMigrations: none
startedAtUtc: 2026-08-22T01:44Z
completedAtUtc: 2026-08-22T01:44Z
executor: mposition
approver:
result: 통과
frozen: false
digest:
---

# Assistant profile 활성화 — 2026-08-22 / `92148e6`

`feature.assistantProfilesEnabled`를 production에서 켠 기록입니다. **검증
회차가 아닙니다** — 검증은 `2026-08-21__9c91042…`가 staging에서 수행했고 그
기록은 동결돼 있습니다. 이 문서는 그 회차가 남긴 두 조건이 충족됐는지와, 전환
자체가 어떻게 관측됐는지를 적습니다.

`docs/ops/image-generation-staging-verification-records/2026-08-20__cc2614d…`가
"활성화 실행은 별도 기록에 있습니다"라고 적고 그 기록이 쓰이지 않은 채로
남았습니다. 같은 자리를 이번에는 비워 두지 않습니다.

## 실행 환경

| 항목 | 값 |
|---|---|
| 환경 | production |
| 배포 SHA (전체 40자리) | `92148e6da91be6bcd761e33115bb8e71085f4b62` |
| Railway deployment id | `83cdf044-f447-4fa1-9346-44bbe3d9cb22` |
| 배포 완료 (UTC) | 2026-08-22T01:40:51Z (`SUCCESS`) |
| 적용된 migration | 없음. `7bb711f..92148e6`에 `prisma/` 변경 파일 0개 |
| 전환 시각 (UTC) | 2026-08-22T01:44 |
| 전환 경로 | Admin Console → `PATCH /api/admin/app-settings` |
| template revision | 2026-08-21a |
| 실행자 | mposition |

### 왜 이 SHA인가

**전환 시점에 production이 서빙하던 것이 이 SHA입니다.** `92148e6`의 배포가
01:40:51Z에 `SUCCESS`로 끝났고, 전환은 01:44에 일어났습니다.

production은 이후 `160a4f1`로 옮겨 갔습니다(배포 시작 01:55:13Z, 완료
02:38:17Z, deployment `360fea70-3c42-4de0-a41f-8e6a0df1f161`). 그 SHA를 이
기록에 적으면 **아직 존재하지 않던 빌드에서 전환이 일어났다고 말하게 됩니다.**

## 검증 회차가 남긴 두 조건

`2026-08-21__9c91042…`의 판정은 `조건부`였고 조건이 둘이었습니다.

### 조건 (1) — production `audit-integrity`가 `valid: true`

전환 **전**:

```json
{ "configured": true, "valid": true, "checkedEntries": 62,
  "firstInvalidId": null, "message": "The HMAC audit chain is valid." }
```

staging에서 관측된 `valid: false`는 staging 키 설정에서 온 것으로 진단됐고,
production에는 그 상태가 없습니다.

**판정**: **충족** (mposition, 2026-08-22)

### 조건 (2) — 검증한 RC가 production에 배포된 뒤에만 전환

**SHA로는 충족될 수 없습니다.** main으로 가는 릴리스가 squash되므로 RC
`9c91042`는 main 이력에 나타나지 않고, `/api/build-info`도 그 값을 돌려주지
않습니다. `git merge-base --is-ancestor 9c91042 92148e6`은 거짓입니다.

그래서 **내용으로** 대조했습니다. 그 회차가 검증한 세 PR(#700·#705·#708)이
건드린 파일 전부입니다.

```
IDENTICAL  app/(site)/(application)/chat/ChatPageClient.tsx        (#700, #705)
IDENTICAL  app/api/chat/route.ts                                   (#708)
IDENTICAL  tests/e2e/assistant-profile-create-adopts-models.spec.ts
IDENTICAL  tests/chatSystemPromptContract.test.mjs
```

`git diff 9c91042 92148e6 -- <위 네 파일>`이 비어 있습니다. 조건이 막으려던
것은 "#700·#705·#708 이전 코드에서 어시스턴트로 시작한 대화의 첫 turn이
실패한다"였고, 그 세 변경은 배포된 트리에 바이트 동일하게 있습니다.

**판정**: **충족** (mposition, 2026-08-22)

## 전환 관측

### AdminAuditLog

| 시각 (UTC) | action | target | 요약 |
|---|---|---|---|
| 01:44 | `app_settings.update_started` | AppSettings / public | Started platform defaults and feature-flag update. |
| 01:44 | `app_settings.guest_default_model.updated` | AppSettings / public | Updated platform defaults and operational feature flags. |

`update_started`의 `metadata`:

```json
{
  "aiChatEnabled": true,
  "attachmentsEnabled": true,
  "guestDefaultModelId": "gpt-5-6-luna",
  "publicSharingEnabled": true,
  "imageGenerationEnabled": true,
  "assistantProfilesEnabled": true,
  "assistantKnowledgeEnabled": false,
  "externalConversationImportEnabled": true
}
```

함께 확인한 것:

- **`assistantKnowledgeEnabled`가 `false`로 유지됐다.** 같은 PATCH에 실려 오는
  별개 flag이고, 정책 §15.1이 knowledge를 다음 순서(#4)로 두었다
- `guestDefaultModelId`가 `gpt-5-6-luna`로 유지됐다 — brand trio 선두 정책과 일치
- 두 행이 같은 실행자·같은 target이며 시작과 완료로 짝을 이룬다

### 감사 체인

| 시점 | `checkedEntries` | `valid` | `firstInvalidId` |
|---|---|---|---|
| 전환 전 | 62 | true | null |
| 전환 후 | 64 | true | null |

두 행만큼 늘고 체인이 유효합니다. 전환이 해시 체인에 편입됐고 기존 62개가
그대로라는 뜻입니다.

## 이 기록이 확인하지 않은 것

- **제품에서 실제로 동작하는지.** 전환 직후 production 계정으로 어시스턴트를
  만들거나 붙여 본 관측이 이 기록에 없습니다. 확인했다면 아래에 적고, 하지
  않았다면 `미기록`으로 둡니다 → **미기록.** 이 회차는 전환과 그 두 조건만
  판정했다. 제품에서 어시스턴트를 만들거나 붙여 본 관측은 이 기록에 없다
- **사용자 영향.** 전환 시점 이후의 생성 수·오류율을 보지 않았습니다
- staging 회차가 이미 판정한 26개 blocking 항목. 그것은 `9c91042` 기록에 있고
  여기서 다시 판정하지 않습니다

## 검증 회차의 발견 사항 (9건) — 이 전환 시점의 상태

어느 것도 전환을 막지 않았습니다. 상태만 적습니다.

| # | 발견 사항 | 상태 |
|---|---|---|
| 1 | §C의 "옛 revision으로 답한다"가 관측으로 판별되지 않음 | 미기록 |
| 2 | superseded 표시가 점 하나 | 미기록 |
| 3 | 편집 화면에 저장 버튼이 둘 | 미기록 |
| 4 | 삭제된 어시스턴트를 대화가 표시하지 않음 | 미기록 |
| 5 | audit 검사기에 key epoch 없음 | 미기록 |
| 6 | Admin Console Audit Log에 id 조회 없음 | 미기록 |
| 7 | Railway `totalDuration`이 client 지연을 반영하지 않음 | 검증 회차 한정 관측. 후속 없음 |
| 8 | `app_settings` audit action 이름이 내용을 말하지 않음 | **이번 전환에서 실례가 나왔다.** flag를 켠 행의 이름이 `app_settings.guest_default_model.updated`이고, 실제로 바뀐 것은 `assistantProfilesEnabled`다. "profiles가 언제 켜졌나"를 감사하려는 사람은 guest 기본 모델 이름을 단 행을 찾아야 한다 |
| 9 | 두 행이 전·후가 아니라 시작·완료 | **이번 전환에서 확인됐다.** 두 행의 `metadata`가 같은 요청 body이므로 이전 값이 감사 기록 안에 없다. 전환 전 상태는 이 기록의 본문에만 남는다 |

## 정책 문서 갱신

| 문서 | 변경 |
|---|---|
| `docs/policy/external-conversation-import-and-memory.md` §15.1 | #3 `assistantProfilesEnabled`의 상태란을 `ON (2026-08-22)`으로 채움 (#743) |

## 판정

| 항목 | 값 |
|---|---|
| 결과 (통과 / 조건부 / 실패) | **통과** |
| 조건 (1) 충족 여부 | 충족. production `audit-integrity`가 전환 전 62항목 `valid: true` |
| 조건 (2) 충족 여부 | 충족. SHA로는 성립하지 않으므로 내용으로 판정했다 — #700·#705·#708이 건드린 네 파일이 `9c91042`와 `92148e6` 사이에 바이트 동일 |
| 되돌릴 계획이 필요한가 | 아니오 |
| 후속 티켓 | 검증 회차의 발견 사항 1–6·8·9. 어느 것도 이 전환을 막지 않았고 전부 켠 뒤에 고칠 수 있다 |

## 롤백

flag를 끄면 profile CRUD·runtime이 fail-closed로 닫힙니다(정책 §15). schema
rollback은 계획하지 않으며(additive), 이미 만들어진 profile 데이터의 제거는
별도 운영 결정입니다.

| 항목 | 값 |
|---|---|
| 롤백을 실행했는가 | 아니오. 이 기록 작성 시점까지 롤백 관측 없음 |
| 실행했다면 시각·사유 | 해당 없음 |

## 서명

| 항목 | 값 |
|---|---|
| 실행자 서명 | |
| 실행 완료일 (ISO 8601) | |
| 승인자 서명 | |
| 승인일 (ISO 8601) | |
