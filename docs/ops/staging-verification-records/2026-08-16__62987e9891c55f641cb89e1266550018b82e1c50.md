---
record: staging-verification
checklist: docs/ops/external-import-staging-checklist.md
templateRevision: 2026-08-15c
checklistSourceSha: 62987e9891c55f641cb89e1266550018b82e1c50
runType: formal
environment: staging
deploySha: 62987e9891c55f641cb89e1266550018b82e1c50
deploymentId: 43cebe68-3707-4b23-a771-2d6778700974
artifactDigest: snapshot 6b8e646a-085a-477e-80b1-793d34096ab5
appliedMigrations: 56/56 already applied; this deploy applied none ("No pending migrations to apply"). Latest is 20260815100000_external_import_gemini_provider.
migrationsCompletedAtUtc: 2026-08-16T12:19:32.581Z (prisma migrate deploy confirmed no pending)
startedAtUtc: 2026-08-16T12:30:00Z
completedAtUtc: 2026-08-18T11:17:30Z
executor: @mposition
approver: @mposition
result: failed
frozen: true
digest: 4a0d7f510a54d7113f65e3df7909a781
---

# Staging 검증 실행 — 2026-08-16 / 62987e9

## 실행 환경

| 항목 | 값 |
|---|---|
| **실행 종류** | 정식(`formal`) |
| 환경 | staging |
| **deployment ID 또는 artifact digest** | `43cebe68-3707-4b23-a771-2d6778700974` / snapshot `6b8e646a-085a-477e-80b1-793d34096ab5` |
| **적용된 migration 식별자** | 56/56 적용 완료, 이 배포는 신규 적용 없음. 최신 `20260815100000_external_import_gemini_provider` |
| **migration 완료 (UTC)** | 2026-08-16T12:19:32.581Z |
| **migration이 앱 배포보다 먼저 적용됨** | 예 |
| template revision | 2026-08-15c |
| **checklist source SHA** | `62987e9891c55f641cb89e1266550018b82e1c50` |
| 시작 (UTC) | 2026-08-16T12:30:00Z |
| 종료 (UTC) | |
| 실행자 | @mposition |
| flag 상태 (시작 시점) | off (A절 완료 후 staging에서 on으로 전환) |

**실행 종류를 먼저 적습니다.** 탐색 실행은 결함을 일찍 찾기 위한 것이고,
활성화 승인의 근거로 인용할 수 없습니다. 정식 실행은 **예정된 production
활성화 대상 SHA** 위에서만 성립합니다 — 체크리스트의 "무엇에 대해 실행하는가"
절이 그 기준입니다. 탐색 실행은 서명·동결하지 않으며, `result`에 무엇을 찾았고
무엇을 덮지 않는지 적습니다.

**deployment ID를 SHA와 함께 적습니다.** 같은 SHA가 같은 artifact를 보장하지
않습니다 — 의존성 해석, builder 버전, build 환경이 그 사이에 움직일 수 있습니다.
SHA만 적힌 기록은 "그때 그 빌드"를 지목하지 못합니다.

**checklist source SHA는 배포 SHA와 다른 것이 정상입니다.** 검증 *절차*와 검증
*대상*은 서로 독립입니다. 체크리스트 문서 이력은 `develop` 한 줄기로 두고,
검증 대상은 활성화 후보 artifact(대개 `main` 계열 SHA)입니다. 두 값을 나란히
적으면 이 어긋남이 의도된 구조임을 나중에 설명할 필요가 없어집니다 — 값이
없으면 "왜 이 기록의 항목이 그 배포에 없던 것이냐"는 질문에 답할 수 없습니다.
생성기가 이 값을 채웁니다.

**세 칸이 함께 있어야 의미가 있습니다.** 식별자만으로는 무엇이 적용됐는지는
알아도 언제인지 모르고, 시각만으로는 앱 배포와의 선후를 말하지 못합니다.
순서가 계약인 이유는 A2에서 드러났습니다 — provider CHECK 확장 같은 migration은
넓히는 방향이라 기존 코드와 역호환이지만, **앱이 먼저 배포되면 migration이
적용되기 전의 요청이 제약에서 실패합니다.** 그 실패는 배포 직후 몇 분에만
나타나고 로그에서 원인을 되짚기 어렵습니다.

"아니오"가 실패를 뜻하지는 않습니다. 그 순서로 배포됐다면 그 창에서 무엇이
일어났는지 확인한 사실을 발견 사항에 적습니다.

## 이 기록이 덮는 범위

정식 실행이 통과해도 그것은 **이 코드와 migration 조합이 올바르게 동작한다**는
근거이지, production이 그 상태라는 증명이 아닙니다. 활성화 전에 production에서
따로 확인하고, 그 결과는 `environment: production`인 **별도 기록**에 적습니다.

그리고 동결한 뒤 다음 중 하나가 달라지면 이 기록은 새 대상을 덮지 못합니다 —
활성화 대상 SHA, 적용 migration 집합, import·parser·wizard 관련 코드, 검증
결과에 영향을 주는 flag·런타임 설정, 실제 배포 artifact.

## 검증용 export와 정리

**정리 계획 (실행 전 확정, 사전 조건 8).** 담당자 `@mposition`, 기한 **검증
종료 후 3일 이내**. 아래 "로컬 파일 정리" 칸이 채워지고 확인되기 전에는 이
기록을 동결하지 않습니다.

**서명 정책 (실행 전 확정).** 1인 운영이므로 실행자와 승인자를 같은 사람
(`@mposition`)이 겸합니다. 빈 승인란이 "승인을 받지 않았다"로 읽히지 않도록
의도임을 여기 적어 둡니다. 두 번째 담당자가 생기면 이 줄을 지우고 역할을
나눕니다.

실제 export에는 개인 데이터가 들어 있습니다. **여기에는 그 데이터가 아니라
데이터에 대한 사실만 적습니다** — 계정 이메일, 대화 제목, 본문, 파일 자체는
저장소에도 CI artifact에도 올리지 않습니다.

| 항목 | 값 |
|---|---|
| 계정 종류 | 개인 계정(실제 ChatGPT·Claude·Gemini export 보유) 1개 + 전용 검증 계정 1개. 주소는 적지 않음 |
| evidence ID (익명) |  |
| export 생성 (UTC) | Google Takeout 2026-08-13T13:34:32Z (아카이브 파일명 타임스탬프). ChatGPT·Claude export는 2026-08-13 |
| 형식과 파일 SHA-256 | Takeout ZIP(JSON 형식) 24,645,345 B — `023d1233e73e69e263dc8706684d8f563776c20de682bbe23020d5348c4e39c8`<br>재압축 ZIP 24,306,688 B<br>ChatGPT export ZIP 6,312,428 B<br>Claude export <br>검증용 합성 시료(개인 데이터 없음): `takeout-gems-only.zip` 1,674 B — `8a47e61dbb6da6aa37d7fb0beb38924ac1ad0553b302bf09543fcfd10a24bee1` / `claude-c2-inbound-limit-fixture.json` 1,000,957 B — `ff86e40c6495c1b423a31d76d52571bf30d7ed21867ab4d32a44c5a5a4ed1f03` / `claude-e3-account-deletion-fixture.json` 1,339 B — `7a112c9ddef135586316cbd0433f8700cfbad3c23b46db919ce3e400d1d59d83` |
| 실행자 | @mposition |
| staging 데이터 삭제 (UTC) | **2026-08-18T11:17:30Z** 확인 완료. 주 계정 external import 18건 삭제(11:14:22Z 1차 10건 → 11:17Z 잔여 8건, 전부 200), 확정 데이터 71대화 · 2,845메시지 · 6,207,282 B 전량 제거되어 capacity 0/0/0. admin 지표에서 chatgpt·gemini 소멸, 잔여는 전용 검증 계정의 claude import 2건 · 3대화 · 16메시지 · 19,538 B뿐이며 계정 삭제 예약에 따라 2026-08-25T10:52:08Z에 함께 소멸 예정 |
| 로컬 파일 정리 (UTC) / 확인자 | 2026-08-18T11:22:43Z / @mposition |

정리 대상은 원본 archive만이 아닙니다.

- 압축 해제본과 임시 복사본
- 브라우저 다운로드 사본
- 스크린샷·영상에 찍힌 대화 내용
- staging의 import·conversation 데이터
- 휴지통·동기화 서비스에 남은 사본
- 검증 중 만든 로그와 첨부 증거 안의 개인 데이터

**정리가 확인되지 않은 기록은 동결하지 않습니다.** "검증 후 정리한다"는 문장은
담당자도 기한도 없어서 지켜졌는지 알 수 없습니다. 위 두 칸이 비어 있으면
`frozen: true`로 넘어가지 않습니다.

## 항목별 결과

체크리스트의 A–H 구획을 그대로 옮기고, 각 항목에 다음 중 하나를 적습니다.

- `pass` — 확인함. 증거 참조를 함께 적습니다.
- `fail` — 확인했고 실패했습니다. 후속 티켓을 적습니다.
- `n/a` — 이 실행에서 해당하지 않습니다. 이유를 적습니다.
- `미기록` — 실행하지 않았거나 기록이 없습니다. **나중에 채우지 않습니다.**

| 구획 | 항목 | 결과 | 증거 | 후속 티켓 |
|---|---|---|---|---|
| **사전 조건** | | | | |
| | staging에 **예정된 production 활성화 대상 SHA**가 배포되어 있고, 그 **전체 40자리 SHA와 deployment ID(또는 artifact digest)**를 실행 기록에 적었다. 활성화 대상이 아직 정해지지 않았다면 이 실행은 탐색 실행이며, 기록의 `runType`을 `exploratory`로 두고 서명·동결하지 않는다. | pass | deploySha `62987e9891c55f641cb89e1266550018b82e1c50`(40자리), deploymentId `43cebe68-3707-4b23-a771-2d6778700974`, artifactDigest snapshot `6b8e646a-085a-477e-80b1-793d34096ab5`. 세 값 모두 front matter와 「실행 환경」 표에 기재 | — |
| | staging의 **적용 migration 집합이 활성화 대상의 것과 같다.** SHA가 같아도 migration이 덜 적용돼 있으면 다른 조합을 검증한 것이다. | pass | `prisma migrate deploy` 결과 56/56 적용 완료, 이 배포가 새로 적용한 migration 없음("No pending migrations to apply"). 최신 항목 `20260815100000_external_import_gemini_provider` | — |
| | **migration이 앱 배포보다 먼저 적용됐다.** 적용된 migration 식별자와 완료 시각을 실행 기록에 적는다. 넓히는 방향의 migration은 기존 코드와 역호환이지만, 앱이 먼저 올라가면 migration 적용 전의 요청이 제약에서 실패한다 — 배포 직후 몇 분에만 나타나고 나중에 원인을 되짚기 어려운 실패다. | pass | migration 확인 완료 2026-08-16T12:19:32.581Z로 앱 배포에 선행. 신규 적용분이 0건이므로 provider CHECK 확장을 포함한 A2 migration이 이미 반영된 상태에서 앱이 올라감 — 제약 실패 창 없음 | — |
| | external import 관련 forward migration이 모두 적용되어 있다 (`external_conversation_import_schema`, `external_import_staging_ledger`, `external_import_analytics_events` 포함). baseline은 수정되지 않았다. | pass | 56/56 적용, 미적용 0건. Gemini provider 확장(`20260815100000_external_import_gemini_provider`)까지 포함. baseline 미수정 | — |
| | `feature.externalConversationImportEnabled`가 **off**인 상태로 배포됐다 (§15: flag off 상태에서 먼저 배포 가능해야 한다). A절을 마친 뒤 **staging 에서는** 이 flag를 on으로 두고 나머지를 실행한다 — production flag는 별도 승인 전까지 계속 off다. | pass | 배포 시점 off — A절 4개 항목이 off 상태에서 fail-closed 통과(403 `EXTERNAL_IMPORT_DISABLED`, 진입점 부재). A절 종료 후 Admin Console Platform settings에서 on 전환, audit 2건(2026-08-16 12:34). production flag는 이 실행에서 변경하지 않음 | — |
| | 검증용 일반 계정 1개(비어 있는 import 상태), admin 계정 1개가 있다. | pass | admin 계정 1개(본 실행 주 계정), 일반 계정 1개. 두 번째 계정은 C2-5의 「Tomverse에 저장된 데이터 없음」 빈 상태 확인과 D-2 IDOR 대조(두 번째 계정 404 vs 주 계정 200)에 사용 | — |
| | 검증용 export 파일이 준비됐다. 아래 "검증용 export를 어떻게 준비하나"의 조건(전용 계정, 통제된 최소 데이터, 두 형식, 두 export 사이 변경 없음)을 만족해야 한다. | pass | 네 조건 중 「두 형식」과 「두 export 사이 변경 없음」 충족 — ChatGPT ZIP(6,312,428 B)과 분할 `conversations-000/001.json`, Claude 재-export로 lineage 확인. **「전용 계정」과 「통제된 최소 데이터」는 의도적 미충족** — 실제 개인 계정의 ChatGPT 190개·Claude 17개·Gemini Takeout 50개 대화를 사용. H절이 「저장소의 fixture는 합성본이고 그것이 초록이라는 사실이 여기서 확인할 것을 대신하지 않는다」고 실제 Takeout을 요구하므로 두 조건이 충돌하며 실제 export를 택함. 그 결과 커진 정리 범위는 「검증용 export와 정리」 표가 담당 | — |
| | **정리 담당자와 완료 기한을 실행 전에 정해 기록에 적었다.** "검증 후 정리한다"는 담당자도 기한도 없어 지켜졌는지 알 수 없다. 정리 확인 전에는 기록을 동결하지 않는다. | pass | 이 기록 「검증용 export와 정리」 절 첫 문단에 실행 전 확정 기재 — 담당자 `@mposition`, 기한 검증 종료 후 3일 이내. 정리 확인 전 동결 금지도 함께 명시 | — |
| **A. Fail-closed (flag off)** | | | | |
| | `GET /api/imports/external/capacity`가 403 `EXTERNAL_IMPORT_DISABLED`를 반환한다. | pass | status 403, code EXTERNAL_IMPORT_DISABLED | |
| | `/settings/imports`가 비활성 안내를 표시하고 파일 선택 UI가 없다. | pass | evidence/A2-settings-imports-disabled.png | |
| | 계정 설정 Data 탭에 "다른 AI 서비스에서 가져오기" 진입점이 보이지 않는다(게스트 대화 가져오기 섹션과 혼동 없음). | pass | evidence/A3-data-tab.png — 외부 import 없음, 게스트 섹션 있음 | |
| | 기존 chat 흐름(전송·comparison·attachment)에 회귀가 없다. | pass | 전송 1 / 2모델 comparison 1 / 첨부 1건, 이상 없음 | |
| **B. 활성화와 기본 흐름** | | | | |
| | Admin Console → Platform settings의 "External conversation import" 토글로 flag를 켠다. audit 로그에 app_settings 갱신이 남는다. | pass | audit 2건(2026-08-16 12:34, tommy.sw.han@gmail.com, targetType AppSettings / targetId public): app_settings.update_started, app_settings.guest_default_model.updated. 두 이벤트의 metadata에 요청 body 전체가 담겨 externalConversationImportEnabled: true 확인. **flag 전용 audit action은 없음** — route가 위 두 개만 기록하고 setExternalImportEnabled 등 flag 4종은 자체 이벤트 없음(app-settings/route.ts:107,122,128). | 발견-11 |
| | Data 탭에 진입점과 사용량 요약이 나타나고 /settings/imports(관리 화면)로 이동한다. "새로 가져오기"가 /settings/imports/new Wizard를 연다. | pass | Data 탭 "데이터 및 개인화" 그룹에 "다른 AI 서비스에서 가져오기" 진입점 표시(A-3의 부재 확인 → flag on 후 출현). 설명에 ChatGPT·Claude·Gemini(Google Takeout) 3종 명시. "가져오기 관리" → /settings/imports 이동 확인, "+ 새로 가져오기" → /settings/imports/new Wizard 열림 확인. 사용량 "가져온 대화 6개 · 0.0 MB 저장됨" — 관리 화면 34.6 KB와 불일치. 원인: AuthButton.tsx:1743이 MB 고정 계산, importFormatting.ts의 formatBytes() 미사용. 1 MB 미만 전량 0.0 MB 표시. | 발견-12 |
| | Wizard 1단계에서 ChatGPT·Claude 내보내기 안내 카드와 "파일이 아직 없어요 / 이미 있어요" 두 경로가 보이고, 개인정보 전체 설명이 접근 가능한 disclosure 뒤에 있다. | pass | **안내 카드 3장**(ChatGPT / Claude / Gemini(Google Takeout)) 각 3단계 표시 — 항목 문구는 2종만 적고 있으나 실제 UI는 3종이며 UI가 더 최신. 두 경로 버튼 표시. disclosure "개인정보 처리 자세히 보기" 기본 접힘, **키보드(Tab→Enter)로 펼침 확인**, privacyNote 전문 노출(기기 미이탈·정규화 텍스트만 저장·미디어 미포함·원 서비스 데이터 미삭제, locales/ko.ts:1097과 일치). 하단 3줄 표시, 형식 줄에 "Gemini 내보내기는 JSON 형식이어야 하며 HTML은 읽을 수 없습니다" 명시. | |
| | ChatGPT export **ZIP**을 선택하면 브라우저에서 파싱되고(Network에 원본 archive 업로드 없음 — §5.1), 대화 선택 단계에 대화 수·메시지 수·예상 저장량·남은 공간·경고가 표시된다. | pass | ZIP 6.02 MB(6,312,428 B). Network 15건(staging 11/GA 4), staging 요청 본문 최대 476 B·합계 2,341 B = ZIP의 0.037%. 본문 있는 요청은 analytics 이벤트 5건뿐(대화 내용 미포함). imports/external·batches·finalize·seal 호출 없음. Worker 로드 확인, worker발 대용량 요청 없음. parse_completed(chatgpt) 12:49:57.451Z. 선택 화면: 대화 190개 / 약 1.5 MB / 남은 공간 약 50.0 MB / 비텍스트 47개 제외 경고 표시. **선택분 전체 메시지 수는 요약에 없고 대화별로만 표시.** HAR·스크린샷: <제한 접근 경로> | 발견-13 |
| | 안내에서 ChatGPT를 고르고 Claude export를 올려도 실패하지 않고 "Claude 기준으로 진행합니다" 비차단 안내만 표시된다. | pass | 안내 카드에서 ChatGPT 선택 후 Claude export 업로드 → 파싱 정상 진행, 차단·오류 없음. 헤더 "감지된 내보내기: Claude", ⓘ 비차단 안내 "Claude 내보내기 파일로 확인되어 Claude 기준으로 진행합니다." 표시(external-import-provider-mismatch). 선택 화면 정상 도달: 대화 17개 · 약 165.7 KB, 비텍스트 479개 제외 경고, 남은 공간 약 50.0 MB. | |
| | 일부만 선택해 "선택 내용 확인" → "대화 N개 가져오기"로 확정하면 완료 요약과 함께 목록·용량이 갱신된다. | pass | 190개 중 6개 선택해 확정. 관리 화면: "50.0 MB 중 34.6 KB 사용", "가져온 대화 2000개 중 6개". 목록에 6개 표시(각 provider·메시지 수·날짜). 선택 화면 대화별 합계 78 메시지·34.629 KB와 실제 34.6 KB 일치(차이 0.08%, 표시 반올림 범위). | |
| | 검색·날짜 필터를 걸어도 화면 밖 선택이 유지되고 "선택 N개 중 M개는 현재 필터에 표시되지 않음" 안내가 보인다. 필터를 지우면 이전 선택이 그대로 복원된다. | pass | 필터 적용 → 목록 2건으로 축소, 요약 "대화 190개 선택" 유지. 안내 표시: "선택 190개 중 188개는 현재 필터에 표시되지 않습니다"(190−188=2, 화면 표시 수와 일치). 필터 지우기 → 190개 전체·체크 상태 복원. | |
| | conversations.json 단독 파일과 Claude export로도 같은 흐름이 된다. | pass | **Claude export**: 17개 중 5개 확정 → 목록에 출처 "Claude" 표시, ChatGPT 6건과 구분(합계 11개/124메시지). **conversations.json 단독**: ChatGPT export가 크기 문제로 conversations-000.json(대화 99개·967.7 KB·비텍스트 6개 제외)과 conversations-001.json(91개·597.8 KB·41개 제외)으로 분할 제공됨. 각각 단독 업로드 시 "감지된 내보내기: ChatGPT"로 정상 파싱, 선택 화면 동일 구성 도달. 두 파일 합계가 ZIP 전체와 일치(190개 / 1.529 MB / 비텍스트 47개) — 분할 파일 병합 처리 정상. 확정하지 않음(중복 판정은 staging 단계 stagedSummary에서 이루어지므로 선택 화면에 중복 표시 없는 것은 정상). 남은 공간 49.9 MB. | |
| | viewer에서 가져온 대화가 원문 그대로(plain text) 보이고, 원본 모델 라벨이 provenance로 표시된다. | pass | "Flexible Work Collaboration Model" viewer: 헤더에 "ChatGPT 원본 모델: text-davinci-002-render-sha", assistant 메시지마다 동일 라벨 반복. 라벨은 export의 model_slug 원값. "메시지 14개 중 14개 표시", 페이지네이션 없이 전량 렌더. 역할 구분(사용자/어시스턴트) 표시. **본문 평문 확인: markdown이 렌더되지 않고 `**Understanding Needs:**`의 별표가 문자 그대로 노출됨.** HTML 태그 문자 노출 없음. 목록에도 출처 "ChatGPT" 표기. 가져온 대화는 채팅 사이드바에 미노출(정책 §2 "Conversation 변환" 비포함, 397행). | |
| **C. 한도·중복·truncation 의미론** | | | | |
| | 100,000 code point를 넘는 메시지가 있는 export: preview가 승인 체크박스를 요구하고, 미승인 시 해당 대화가 선택되지 않으며, 승인 후 저장본에 `[[tomverse:truncated]]` marker와 truncation 안내가 보인다. | pass | Claude 형식 경계 시료(user 역할 메시지 100,001 코드포인트 = 상한 +1). 미승인 상태에서 해당 행의 선택 체크박스 `disabled`, 영향 안내(`external-import-row-truncation-impact`) 표시 — 승인 체크박스(`external-import-row-truncation-consent`)를 켠 뒤에만 선택 가능(스크린샷 `C-1-truncation-consent`). 확정 후 저장본을 export API로 재확인: `truncated: true`, 원본 100,001자 → 보존 100,000자, `[[tomverse:truncated]]` marker 포함, `originalContentDigest`가 `contentDigest`와 별도 보존. 보존값이 저장 상한과 정확히 일치 — marker 26 코드포인트를 뺀 99,974를 head 74,980(75%) + tail 24,994로 나눈 결과로 `externalImportLimits.ts:201-215`의 계약과 일치. admin 지표 truncatedMessages 1(F절)과, C3-2 재시작 후 truncation 동의 유지(C3절)와도 부합 | — |
| | 1,000,000 code point를 넘는 메시지가 있는 대화는 "가져올 수 없음"으로 표시되고 선택 자체가 불가능하다(§5.3 — 메시지 단위 누락 없음). | pass | 합성 Claude export(대화 2건, 개인 데이터 없음) — 대화 1은 user 메시지 1,000,001 코드포인트(상한 +1), 대화 2는 짧은 대조군. 목록 위 요약 「1개 대화에 가져오기 크기 한도를 넘는 메시지가 있어 가져올 수 없습니다.」, 해당 행 차단 사유 「메시지 1개가 가져오기 한도를 넘어, 해당 메시지만 빠지는 것이 아니라 이 대화 전체가 제외됩니다.」, 선택 체크박스 `disabled`. 대조 대화는 선택 가능(선택 요약 「대화 1개 선택 · 약 83 B」)하여 차단이 해당 대화에만 적용됨을 확인(스크린샷 `C-2-inbound-limit`). 3단계까지만 진행해 서버 행 미생성. 구현 근거 — 메시지 1건이라도 상한 초과 시 대화 전체가 `not_importable`(`externalImportPipeline.ts:119-121`), `isRowSelectable()`이 선택 차단(`externalImportWizard.ts:323`) | — |
| | 같은 export를 다시 가져오면 전부 "중복 제외"로 skip되고 저장량이 늘지 않는다(§4.2 완전 일치). | pass | conversations-000.json 단독 업로드 후 기존 6개만 선택 → 4단계에서 "선택한 대화가 모두 이미 계정에 있습니다"(allDuplicates, ImportReviewStep.tsx:53). 가져오기 버튼 미제공, "다른 내보내기 가져오기"만 제공되어 확정 불가. 저장량 변동 없음. 최초 import는 ZIP, 재-import는 단독 JSON — 동일 6개를 중복 판정하여 stable ID가 컨테이너 형식에 비의존. | |
| | 대화가 추가된 최신 export를 가져오면 새 snapshot이 생기고, 목록이 같은 lineage로 묶어 최신본을 앞에, 이전 버전을 disclosure 뒤에 표시한다. 이전 snapshot을 개별 삭제할 수 있다(§4.2). | pass | Claude에서 기존 대화 2건에 메시지 추가 후 재-export → 재-import. "Claude Code 대화 이어서 하기" 18→19개, "서비스나우 카타로그 폼 셀렉트 옵션 구현" 12→13개. 각 행에 "이전 버전 1개" 토글(external-import-lineage-toggle) 출현, 최신본 앞·이전 버전 disclosure 뒤. 버전 1개뿐인 "Tommy Han Brisbane Inquiry"에는 토글 없어 구분 확인. 토글 펼침 → 이전 snapshot viewer 진입 → **개별 삭제 실행, 정상 삭제되고 최신본은 유지됨 확인**. Claude export로 실행(체크리스트 provider 미지정, 한도·truncation은 externalImportPipeline.ts 공용 경로). | |
| | (선택) 잔여 quota를 초과하는 선택을 시도하면 업로드 전에 UI가 차단하고, 서버 finalize 강행 시 409 EXTERNAL_IMPORT_QUOTA_EXCEEDED all-or-nothing 거부가 확인된다. admin 지표의 quota_rejected 카운터가 증가한다. | n/a | 항목이 (선택)으로 표시됨. 50 MB quota를 실제로 채우려면 사전 조건의 "통제된 최소 데이터" 원칙과 충돌하고, admin에서 한도를 낮추는 것은 검증 중 런타임 설정 변경이라 동결 무효 조건 4에 해당하므로 실행하지 않음. | |
| **C2. Seal · 재개 · 만료 (§5.5)** | | | | |
| | 확인 화면에 도달하면 status가 preview_ready이고, 같은 선언으로 seal을 다시 호출하면 200 idempotent replay가 온다 | pass | 콘솔 실행 로그 C2-1 (200, idempotentReplay:true) | — |
| | 선언을 바꿔 seal을 호출하면 409 EXTERNAL_IMPORT_SELECTION_CHANGED이고 상태는 그대로다 | pass | 콘솔 실행 로그 C2-2 (409 + 이후 status=preview_ready) | — |
| | preview_ready import에 batch를 더 보내면 409로 거부된다 | pass | 콘솔 실행 로그 C2-3 (409 EXTERNAL_IMPORT_SELECTION_CHANGED) | — |
| | 확인 화면에서 일부 대화를 해제하고 확정하면 선택한 것만 저장되고 나머지 staged row는 삭제된다(부분집합 finalize) | pass | evidence/C2-4-subset-*.png, 콘솔: status=completed conversations=2 rows=2 | — |
| | Wizard 중간에 브라우저 뒤로가기를 누르면 관리 화면으로 나가고, 서버 import가 아직 없으면 "Tomverse에 저장된 데이터 없음"이 보인다. beforeunload 경고가 뜨지 않는다 | pass | evidence/C2-5-back-navigation.png — 빈 상태 확인용 별도 검증 계정, 진행 중 카드 0건 / noServerDataYet 표시 | — |
| | 관리 화면 진행 중 카드: preview_ready에만 "이어서 완료하기"가 있고, seal되지 않은 staging에는 다시 시작·삭제만 있다 | pass | evidence/C2-6-in-progress-cards.png | — |
| | TTL이 지난 작업은 조용히 사라지지 않고 "만료되어 다시 시작해야 함"으로 표시된다 | 미기록 | 관측 창을 놓침. `expired`는 열린 상태에서만 계산되고(`externalImportService.ts:266-289`) sweep이 15분마다 `failed`로 넘기므로 관측 가능 구간이 최대 15분인데, 만료 예상 시각(2026-08-18 06:47~08:01 UTC) 기준 약 1시간 30분 뒤에 확인함. UI 자체는 `tests/e2e/external-import-settings.spec.ts:1248`이 mock 응답으로 덮으나 staging 실측이 아님 | — |
| | 15분 maintenance sweep이 `staging`과 `preview_ready`를 모두 정리한다 | pass | 2026-08-17에 열려 있던 import 6건(admin 지표 `active: 6` — claude 4 / chatgpt 2)이 2026-08-18T09:31Z 확인 시점에 전부 `failed` / `EXTERNAL_IMPORT_STAGING_EXPIRED`로 전환됨. provider 분포 일치. 두 열린 상태가 모두 포함됨 — `preview_ready` `…ia3hs7`(08-17T08:01Z 생성)·`…wyeg81`(07:51Z), `staging` `…77x7oq`(06:56Z)·`…v189tv`(06:47Z). sealing이 TTL을 연장하지 않는다는 §5.5 계약이 실측으로 확인됨. 전환 후 `expired`·`resumable` 모두 false가 되어 진행 중 목록에서 빠짐 | — |
| | seal 없이 staging에서 바로 finalize를 호출하면 409로 거부되고, 같은 요청이 seal 이후에는 성공한다 | pass | 콘솔 실행 로그 C2-8a(409) / 8b(200) / 8c(200) | — |
| **C3. 용량 부족 복구 (§5.3)** | | | | |
| | 대화 선택 단계에서 선택량과 남은 공간이 나란히 보이고, 초과하면 진행 버튼이 잠긴다 | pass | evidence/C3-1-quota-lock.png — 190개·1.5MB 선택 vs 남은 공간 1.0MB에서 quota 경고 + 「선택 내용 확인」 비활성, 선택 축소 시 복구 | — |
| | 업로드 중 서버가 quota로 거부하면 같은 내용 다시 시도 버튼이 없고 선택을 줄이라는 안내만 나온다. 승인된 batch가 하나라도 있었다면 기존 staging이 삭제되고 새 import로 다시 시작한다는 안내가 함께 보이며, 로컬 선택과 truncation 동의는 유지된다 | pass | evidence/C3-2-quota-restart.png (B분기: external 201 / batches 200 / batches 409 / DELETE 200, 재시작 안내 있음), evidence/C3-2-quota-revise.png (A분기: external 201 / batches 409, 재시작 안내 없음). 양쪽 모두 선택 190개 유지, 「업로드 다시 시도」 없음 | 발견-1, 발견-2 |
| | 네트워크를 잠깐 끊어 전송을 실패시키면 같은 sequence 재시도 버튼이 나오고, 재시도 시 이미 도착한 batch가 중복 생성되지 않는다 | pass | evidence/C3-3-transient-retry.png · sequence 2에서 offline 실패 → 재시도 시 sequence 2,3만 전송 → step 4 도달(seal이 staged id 목록·개수를 대조하므로 중복 시 409) | — |
| | finalize에서 quota로 거부되면 아무것도 저장되지 않고 확인 화면으로 돌아와, 선택을 줄여 재업로드 없이 다시 확정할 수 있다 | n/a | batch-time guard가 staged+finalized 합계를 검사하므로 마지막 batch가 통과하면 finalize는 항상 통과. 제품 API로 도달 불가. 해당 경로는 tests/integration/external-import-lifecycle.db.test.ts:940이 raw SQL로 상태를 구성해 검증 | 발견-3 |
| **D. 보안 spot check** | | | | |
| | <script>·onerror 속성이 든 HTML을 본문에 포함한 합성 export를 가져와 viewer에서 문자 그대로 렌더링되는지 확인한다(§19 XSS) | pass | evidence/D1-xss-viewer.png · payload 10종(script/img onerror/svg onload/javascript: href/iframe/이중 escape/markdown/breakout/style/body onload)이 제목 4개 화면과 viewer 본문에서 문자 그대로 렌더. DOM 카운트 script·img[onerror]·iframe·svg[onload]·style·javascript: href 모두 0, alert·CSP 위반 없음 | — |
| | 다른 계정으로 importId·conversationId를 직접 조회하면 404가 온다 (IDOR — 존재 여부가 새어 나가지 않음) | pass | 주 계정 200 / 두 번째 계정 404, 응답이 없는 ID와 동일({"code":"NOT_FOUND"}). /settings/imports/conversations/{남의 id} 페이지도 찾을 수 없음 | — |
| | 서버 구조화 로그를 열람해 filename·대화 제목·본문·외부 ID·digest·fingerprint가 로그에 없음을 확인한다(§19/§22 content-free) | pass | evidence/D3-railway-logs.json — 2026-08-17T06:20:30Z~07:30:58Z, service Tomverse. 앱 로그는 external_import_counter ×2(event/kind/count만), auth.sign_in/out, CSP incident ×2, provider probe(무관)뿐. 금지 항목 0건. 해당 구간에 190건 ChatGPT 업로드·XSS 합성 import·viewer 열람·IDOR 조회가 모두 포함되며 성공 경로는 무로그. "external import ... failed" 줄 0건 | — |
| **E. 삭제·export·계정 정리** | | | | |
| | JSON export 다운로드가 동작하고 provenance(digest·모델 라벨·시각)가 포함된다. flag를 꺼도 다운로드와 삭제가 계속 동작한다(§15 rollback) | pass | evidence/E1-export-provenance.txt, evidence/G1-flag-off-probe.txt — UI 다운로드·열람 정상, format=tomverse.external-conversations.v1·digestVersion=1·generatedAt, 대화 22건(목록 일치), conversationDigest/sourceModelLabels/sourceCreatedAt/sourceUpdatedAt/importedAt 및 메시지별 contentDigest/originalContentDigest/sourceModelLabel/sourceTimestamp 존재(Claude 모델 라벨은 값 null, 필드 부재 아님). flag OFF: export API 200, 대화 삭제 200 deleted | 발견-6 |
| | import 전체 삭제·snapshot 개별 삭제 후 목록·용량·상세 수치가 서로 맞는다 | pass | evidence/E2-delete-audit.txt — 용량 API usage, 대화 목록 행 합계, 완료 import 상세 conversationCount 3자 교차검증. 기준 42,119,200 bytes / 29건 (3자 일치, 상세 불일치 0). snapshot 개별 삭제 후 32,219,200 / 28건. import 전체 삭제(대화 3건) 후 11,087,630 bytes / 25건, outcome=deleted. 모든 단계에서 3자 일치·상세 불일치 0 | 발견-1 |
| | 계정 삭제 절차 — 예약·세션 폐기·유예 기간 데이터 보존 | pass | 사용자 계정 삭제 절차 실행. 확인 대화상자에 즉시 로그아웃·대기 중 접근 차단·7일 후 영구 삭제·취소는 셀프서비스 아님 4개 항목 표시, 체크박스 + `DELETE MY ACCOUNT` 이중 확인(스크린샷 `E-3-delete-dialog`). 실행 후 `accountStatus: pending_deletion`, `accountDeletionRequestedAt: 2026-08-18T10:52:08.891Z`, `accountDeletionScheduledFor: 2026-08-25T10:52:08.891Z`(정확히 7일), `aiUsageRestricted: true`, Active sessions 0(스크린샷 `E-3-pending-deletion`). 유예 중 데이터 보존 확인 — admin 지표가 예약 전후 동일(imports 20 / claude 11 · 15 · 147 · 470,975). 재로그인 차단 동작하나 사유가 잘못 표시됨 | 발견-15 · 발견-16 |
| | 영구 삭제 시 Import·대화·메시지가 함께 사라진다(§13.1) | 미기록 | 이 회차 안에서 관측 불가. 제품 경로는 7일 유예 후 maintenance가 `deleteTomverseAccount`를 실행하므로 2026-08-25T10:52Z 이전에는 일어나지 않고, Admin Console의 즉시 삭제는 1인 운영에서 승인 관문에 막힌다(발견-17). 삭제 대상 계정에는 검증용 데이터만 있으며 확정분은 claude import 2건 · 대화 3건 · 메시지 16건 · 19,538 bytes로 확인됨(삭제 시 admin 지표가 imports 18 / claude 9 · 12 · 131 · 451,437로 떨어져야 함). cascade 자체는 항목이 지목한 `tests/integration/account-deletion.db.test.ts`와 schema의 `onDelete: Cascade`(ExternalImport→ExternalConversation→ExternalMessage)가 덮으나, 그것이 staging 실측을 대신하지 않음 | 발견-17 |
| **F. 관측·운영** | | | | |
| | GET /api/admin/external-imports가 provider·parserVersion 분해, bucket, 중복·truncation 비율, 카운터를 반환하고 값이 위 검증 활동과 부합한다. parserVersion이 배포된 값으로 기록된다 | pass | evidence/F1-admin-metrics.json (2026-08-17T07:4x) — finalizedConversations 22·finalizedBytes 548,654가 capacity·목록과 일치, quota_rejected 2가 C3-2 및 Railway 로그와 일치(2026-08-17 당일 조회값 —
이 카운터는 익일 삭제되므로 재현 불가, 발견-14), truncatedMessages 1이 C-1과 일치, parserVersion=v3(제품 경로 12건). c2-verify 2건은 검증용 API 직접 생성 import로 제품 경로 아님 | — |
| | external_import_step_entered / _abandoned가 기록되고 import_step 외의 속성이 없다 | pass | evidence/F2-analytics-payload.json — _step_abandoned 원문 properties={market_tier, paid_marketing_eligible, experiment_variant, import_step:"server_review"}. import 고유 속성은 import_step 하나이며 파일명·대화 수·바이트·제목 없음. 공통 마케팅·실험 속성과 봉투 필드(client_id/session_id/utm_/language/country)는 전 이벤트 공통. 소스 확인: _parse_completed·_parse_failed·_desktop_recommended·_finalized도 import_provider(및 실패 사유)만 전송 | 발견-5 |
| | 15분 maintenance 결과에 `externalImportStaging` sweep 수치가 보인다 (중도 이탈 import를 만들어 두고 다음날 만료 처리를 확인하는 것은 선택 항목). | pass | 2026-08-18T09:3xZ admin 지표 — `counters.staging_expired` 6, 어제 기준선 0에서 상승. `imports.active` 0, `failed` 6, `failureCodes` `{"EXTERNAL_IMPORT_STAGING_EXPIRED": 6}`로 sweep이 처리한 건수와 일치. 선택 항목인 익일 만료 확인도 수행함(C2 TTL 참조). 단 이 카운터는 당일치만 남는다 — 발견-14 | 발견-14 |
| | 모바일 기기(또는 시뮬레이터)에서 320px 폭 레이아웃이 깨지지 않고, 대형 archive에서 데스크톱 권장 안내가 표시된다(가능한 경우) | pass | evidence/F4-320px.txt — Responsive 320px에서 관리 화면·wizard 1~4단계·viewer 6개 화면 모두 documentElement.scrollWidth === clientWidth === 320, 가로 오버플로 0. 데스크톱 권장 안내는 worker OOM 조건이라 이 실행에서 재현하지 않음(체크리스트 「가능한 경우」 단서) | — |
| | ko/en 두 locale로 대표 화면(설정·preview·viewer)을 렌더링해 확인한다(§16 — 나머지 5개 locale은 정적 parity 테스트로 갈음) | pass | evidence/F5-locales-*.png — 설정(관리 화면)·preview(wizard 4단계)·viewer 3개 화면을 ko/en으로 렌더. 번역 적용, 키 노출 없음, 레이아웃 유지 | — |
| **G. Rollback drill** | | | | |
| | flag를 다시 끄면 A 항목의 fail-closed 상태로 즉시 돌아가고, 이미 가져온 데이터는 목록·삭제·export로 계속 접근 가능하다 | pass | evidence/G1-flag-off-.png + probe 로그. 차단: POST 새 import·capacity·import 상세·대화 목록 모두 403 EXTERNAL_IMPORT_DISABLED, wizard·Data 탭 항목·「새로 가져오기」 부재, 상세 페이지는 안내 문구로 처리. 유지: import 이력 200·export API 200·삭제 200(deleted). 계정 전체 데이터 내려받기(가져온 기록 포함)는 Data 탭에 계속 노출 | 발견-6 |
| | drill 종료 후 flag 상태를 운영 결정에 맞게 되돌려 놓았다 | pass | ON으로 복구. H·H2 및 익일 TTL·sweep 항목이 남아 있어 staging 검증 계속을 위해 ON이 맞는 상태. production 활성화 승인과 무관 | — |
| **H. Gemini(A2) import** | | | | |
| | Wizard 1단계에 Gemini 안내 카드가 있고, 문구가 My Activity → Gemini Apps와 JSON 형식을 지목한다. Takeout에서 "Gemini" 항목을 그대로 고르면 대화가 아니라 Gems를 받게 된다는 사실이 읽힌다 | pass | evidence/H1-gemini-guide-card.png — 제목 「Gemini(Google Takeout)」, 3단계 안내에 「내 활동 → Gemini 앱」·「형식을 JSON으로」·「Gemini 항목 자체에는 대화가 아니라 Gem이 들어 있습니다」 명시 | — |
| | 실제 Takeout ZIP 브라우저 파싱 / provider Gemini 인식 / 원본 archive 업로드 없음 | fail | 원본 Takeout ZIP 업로드 시 `html_export_unsupported` 안내(스크린샷 `H-2-original-takeout`). 동일 내용 재압축 ZIP은 provider `gemini`로 인식, 대화 50개·저장 메시지 2,606(스크린샷 `H-2-rezipped-preview`). 아카이브 비업로드는 별도 충족 — HAR `H-2-network.har`: `staging.tomverse.app` request body 합계 6,672 bytes(`POST /api/analytics/events` 14건이 전부), `POST /api/imports/external`·`/batches` 0건, 08:56:35.638Z `file_selection` → 08:56:43.975Z `parse_completed(import_provider="gemini")` 구간 서버 요청 없음. 대화 JSON 항목 압축 해제 기준 5,531,336 bytes | 발견-7 |
| | 한국어 경로 계정 / 영어 경로 계정 동일 결과 | 미기록 | 영어 경로 Takeout 계정 없음 | 발견-10 |
| | 대화에 `.zip`을 첨부한 계정의 export가 거절되지 않고 preview가 그 수를 말한다 | pass | 재압축 Takeout에 중첩 `.zip` 4개(`New Mexico-*.zip` ×3, `tomverse_favicon_pack-*.zip` ×1) 포함. 아카이브 거절 없이 대화 50개 파싱 완료, preview에 「첨부된 압축 파일(.zip 등) 4개」 별도 줄 표시(스크린샷 `H-2-rezipped-preview`). 중첩 아카이브는 열지 않고 skip — `externalImportArchive.ts:120-128` | — |
| | preview의 네 수치가 각각 따로 보인다 | pass | 같은 화면에 다섯 줄이 각각 별도 표시 — 텍스트가 아닌 항목 287 / 첨부된 압축 파일 4 / 아카이브에 없는 첨부 38 / 분기 중복 메시지 1,434 / 어느 대화에도 속하지 않은 항목 2. 전부 원본 JSON 독립 재계산과 일치. 네 수치 중 「서식을 읽지 못해 제외한 답변」은 실제 값이 0이라 미표시(`ConversationSelectionStep.tsx:355-424`는 `> 0`일 때만 렌더링) — 이 export가 해당 경우를 담고 있지 않음을 adapter 재실행으로 확인(`skippedUnrecognizedContent` 합계 0) | — |
| | 분기된 대화가 각각 독립 대화로 들어오고 선택 화면에서 개별 해제 가능 | pass | 재압축 Takeout 50개 대화 중 공유 turn 기준 분기 묶음 3개(크기 4/2/2, 8개 대화). 묶음 A 4개(`88c066cc…`/`bc0ab846…`/`defd5439…`/`b449e265…`, 각 280·282·278·276 메시지)를 개별 해제 → 50개 5.4 MB에서 46개 3.5 MB. 그중 1개만 재선택 → 47개 4.0 MB이며 나머지 3개는 해제 상태 유지(스크린샷 `H-5-deselect-4`, `H-5-reselect-1`). 공유 메시지 합계 1,434는 화면의 「분기 중복 메시지 1,434개」와 일치 | — |
| | Gemini 답변이 viewer에서 안전하게 렌더링 (D절 XSS 확인을 Gemini 대화로 재수행) | pass | 실제 HTML 소스(`<head>`, `<link rel="icon" href="/favicon.ico">`, `apple-touch-icon`)를 담은 대화(메시지 10개·13.7 KB)를 viewer에서 열어 확인. 양성 대조 — 본문에 `rel="icon"` 문자열 존재, DOM 소스에는 `&lt;head&gt;`로 이스케이프, `<link rel=icon>` 날것 삽입 없음. 주입 요소 script/iframe/link/style/img/on* 속성 모두 0, `performance.getEntriesByType('resource')`에 favicon류 요청 0건(스크린샷 `H-6-viewer-html-literal`). 구현 근거 `ExternalConversationViewer.tsx:339-343` — React text node | — |
| | 같은 export를 다시 가져오면 전부 중복으로 skip되고 저장량이 늘지 않는다 | pass | 동일 재압축 Takeout ZIP을 2회차 업로드·전체 선택. 4단계에서 확정 버튼 없이 「선택한 대화가 모두 이미 계정에 있습니다」로 종료(스크린샷 `H-7-all-duplicates`, `ImportReviewStep.tsx:45-62`의 staged 0건 분기). capacity는 1회차 확정 직후와 동일한 6,207,282 / 71 / 2,845로 불변. admin `byProvider.gemini` — imports 2 / completed 1 / active 1 / duplicatesSkipped 50 / finalizedConversations 50. 대화 ID는 Gemini chat ID, 메시지 ID는 `chatId:timestamp:role`에서 파생되므로 재현 가능(`gemini.ts:228-233`) | — |
| | 답변이 markdown으로 저장돼 viewer에서 표·코드 블록·목록이 제대로 보인다 | fail | 위와 같은 대화의 저장 내용에 코드 울타리(백틱 3개) 마커 6개(코드 블록 3개)·표 1개·목록 4개·제목 4개가 있으나 viewer DOM의 `pre, code` 요소 수는 0. viewer는 `whitespace-pre-wrap` 평문으로만 렌더링하며 `components/imports/` 전체에 Markdown 컴포넌트 참조 0건. 정책 §4(`external-conversation-import-and-memory.md:129-130`)는 평문과 제한 Markdown 양쪽을 허용하므로 정책 위반은 아니고 체크리스트 항목과 구현의 불일치 | 발견-8 |
| | HTML 태그가 글자 그대로 노출되지 않는다 (동일 항목 후반절) | pass | 50개 대화 전수 스캔 결과 변환 후 태그가 남은 대화 6건이며 모두 코드 블록 내용(TypeScript 제네릭·JSX·HTML 소스 예제). 산문 본문에 노출된 변환 잔여 태그 없음 | — |
| | HTML export 업로드 시 재내보내기 안내와 진단 라벨 | pass | 스크린샷 `H-9-html-guidance`. HAR `H-2-network.har`의 `external_import_parse_failed` 4건(08:45:20 / 08:46:09 / 08:46:13 / 08:47:06) 모두 `import_failure_reason: "html_export_unsupported"` | — |
| | Gems·예약 작업 파일만 담긴 ZIP도 같은 안내로 이어지고 조용히 "대화 0건"으로 끝나지 않는다 | pass | 원본 Takeout에서 `Takeout/Gemini/gemini_gems_data.html`·`gemini_scheduled_actions_data.html` 두 항목만 추출한 ZIP(1,674 B) 업로드. 2단계에서 「HTML 형식으로 내보낸 파일입니다. 내 활동을 JSON 형식으로 다시 내보낸 뒤 그 파일을 올려 주세요.」 표시, 일반 실패 문구(`externalImport.parseFailed`) 아님. 「자세한 오류 정보」 전개 시 진단 라벨 `html_export_unsupported`(스크린샷 `H-10-gems-only-zip`). 경로 — `.html` 항목은 `PARSEABLE_EXTENSIONS`에 없어 skip되고 provider 미결정 + `sawHtmlEntry`로 분기(`externalImportArchive.ts:135-137`, `externalImportWorker.ts:348-354`) | — |
| | 활동 기록이 꺼져 있어 대화가 없는 export는 오류가 아니라 정상 상태로 구분해 표시된다(§1). | n/a | 활동 기록이 꺼진 상태의 Takeout export가 없어 이 실행에서 재현 불가. 합성 fixture로는 H절 전제(실제 Takeout에 대해서만 의미 있음)를 만족하지 못함 | — |
| | admin 지표에서 이 import가 provider `gemini`로 잡힌다 | pass | `GET /api/admin/external-imports?days=7` (2026-08-17T09:40:55Z) `imports.byProvider`에 `gemini` 항목 — imports 1 / completed 1 / finalizedConversations 50 / finalizedMessages 2,606 / finalizedBytes 5,658,666 / truncatedMessages 0 / duplicatesSkipped 0으로 확정 실측과 전부 일치. `byParserVersion`에 배포값 `v3`(16건). 버킷 `conversationBuckets.le-199` 1, `byteBuckets.le-8mb` 1. 창에 남은 `c2-verify`(2)는 C2 검증용 합성 import이며 제품 경로가 아님. 어제 보였던 `c3-fill`은 해당 import 삭제로 창에서 사라짐(완료 import 삭제 시 행 자체가 제거됨 — `externalImportService.ts:1433`) | — |
| **H · H2. 분기가 실제로 얼마를 쓰는지** | | | | |
| | preview에 표시된 분기 중복 메시지 수가 실제 값과 정확히 일치한다 | pass | 화면 1,434 = geminiAdapter.parseAll() 계산 1,434. 저장 예정 2,606 / 순수 추가 972 / 순수 추가 2,068,783 B | — |
| | 분기 중복 메시지 수가 선택분이 아니라 export 전체 기준임을 화면에서 알 수 있다 | 동작 pass / 표시 fail | 동작 — 대화 50개 선택 시와 46개 선택 시(묶음 A 4개 해제, 공유 메시지 1,070 보유) 모두 「분기 여러 개에 함께 있는 메시지 1434개」로 불변. 선택분 기준이면 364가 되어야 함. 나머지 네 줄(287·4·38·2)도 동일하게 불변. 코드 근거 `ConversationSelectionStep.tsx:161` — `state.warnings`는 파싱 시점 총계이며 `state.selectedIds`와 무관. 표시 — 문구에 기준을 밝히는 말이 없고(`locales/ko.ts:1122-1123`), 「포함되지 않는 항목」 헤딩 아래에 있으면서 내용은 「각 분기에 저장됩니다」로 포함을 말함(en도 `Not included` / `stored in each`) | 발견-9 |
| | 분기 해제 시 메시지 수와 예상 용량이 즉시 감소 | pass | 위와 동일. 해제분 4개의 예상 저장량 1,960,187 B가 총계에 그대로 반영(5,658,666 → 3,698,479 B, 화면 5.4 MB → 3.5 MB). 1개 재선택 시 4,184,820 B → 4.0 MB | — |
| | 사용자가 어느 대화가 분기인지 식별 가능 | pass | 목록 행에 분기 배지 없음(`ConversationSelectionStep.tsx:504-586`). 제목의 「분기된 」 접두사는 Tomverse가 아니라 원본 Takeout `title` 필드의 값이며 계정 언어로 지역화됨(저장소 내 해당 문자열 0건). 라벨 대 실제 묶음 대조: 일치 6, 과대보고 0, 과소보고 2(분기의 원본 대화). Tomverse 측 표시는 목록 상단 「분기 중복 메시지 1,434개」 총합뿐 | — |
| | 확정 후 실제 저장량이 예측한 양과 일치 | pass | 확정 전 기준선 `normalizedTextBytes` 548,616 / 대화 21 / 메시지 239. 재압축 Takeout 50개 대화 확정 후 6,207,282 / 71 / 2,845 — 증가분 5,658,666 B / 50 / 2,606이 preview 예측값과 정확히 일치(스크린샷 `H2-5-capacity-before`, `H2-5-capacity-after`). 이 export는 절단 대상 0·차단 대상 0(최대 메시지 27,905 코드포인트 < 상한 100,000)이라 client 근사가 개입하지 않음 | — |

증거는 스크린샷 경로, 로그 발췌 위치, admin 응답 캡처처럼 **다시 확인할 수
있는 것**을 적습니다. 개인 데이터가 있는 자료는 저장소가 아니라 제한된 운영
저장소에 두고 여기에는 참조만 남깁니다.

## 판정

| 항목 | 값 |
|---|---|
| 결과 (통과 / 조건부 / 실패) | **실패** — release blocker 1건(발견-7)이 확인됨. 이 실행은 외부 대화 가져오기의 production 활성화 근거가 되지 못한다 |
| 조건부일 때의 조건 | 해당 없음. `조건부`는 조건 충족 시 이 기록이 활성화 근거가 된다는 뜻인데, 발견-7의 수정은 `lib/externalImportArchive.ts`·`lib/workers/externalImportWorker.ts`를 바꾸므로 「이 기록이 덮는 범위」의 무효 조건인 **import·parser·wizard 관련 코드 변경**에 해당한다. 조건을 만족시키는 순간 이 기록이 새 대상을 덮지 못하게 되므로 조건부는 성립하지 않는다. 활성화 전에는 수정된 SHA에서 A~H/H2 전체를 다시 실행해야 한다(체크리스트 H절) |
| 발견 사항 | 16건(발견-1~17, 발견-4 결번). 아래 「발견 목록」 참조. **release blocker는 발견-7 하나** — Google Takeout ZIP이 data descriptor를 쓰므로 대화 JSON이 빈 파일로 skip되고 HTML export로 오인되어, 정상 경로로 내보낸 사용자 전원이 실패한다. 나머지는 UX·관측·체크리스트 정합성 문제이며 데이터를 잘못 저장하거나 잘못 지우는 종류는 없다. 계정 삭제 흐름에서 발견-15·16이 짝을 이룬다(삭제가 안 됐는데 된 것처럼 보이는 경우와, 됐는데 사유를 말하지 않는 경우) |
| 후속 티켓 | #664 (발견-7, release blocker). 발견-8은 체크리스트 H-8 문구를 정책 §4에 맞추는 문서 PR로 처리 예정(방향 확정, 미발행). 발견-1·2·9·11·12·13·15·16은 UX 후속으로 미발행, 발견-14·17은 관측·운영 후속으로 미발행, 발견-3·5는 체크리스트 정합성 후속으로 미발행, 발견-6·10은 이번 실행의 미검증 범위 기록이며 티켓 대상 아님 |


### 발견 목록

번호는 「항목별 결과」의 후속 티켓 칸이 가리키는 대상입니다. 이슈를 발행하면
해당 항목의 후속 티켓 칸을 이슈 번호로 교체합니다.

| 번호 | 발견 | 인용 행 | 성격 |
|---|---|---|---|
| 발견-1 | 취소된 import의 카운터가 갱신되지 않는다. 열린 import를 취소하면 `finalized: false` 대화 행은 삭제되고 status만 `cancelled`로 바뀌며 `conversationCount`·`messageCount`·`normalizedBytes`는 그대로 남는다(`externalImportService.ts:1401-1415`). 관리 화면 이력이 남은 행 0건인 import를 「취소됨 · 대화 44개 · 484.2 KB」로 표시한다. 용량 회계는 `finalized: true`만 세므로 실제 quota에는 영향이 없다 | C3-2 · E-2 | UX/표시 |
| 발견-2 | 용량 부족 복구 화면의 「남은 공간」이 오래된 값이다. capacity는 wizard mount 시 한 번만 조회하고 이후 갱신하지 않으므로(`ExternalImportWizard.tsx:117-150`), quota 거부 후 선택을 줄이는 화면에서 서버의 현재 잔여량과 다른 수치를 보고 판단하게 된다 | C3-2 | UX |
| 발견-3 | finalize 시점 quota 거부는 제품 API로 도달할 수 없다. batch 시점 guard가 staged + finalized 합계를 검사하므로(`externalImportService.ts:1016-1032`) 마지막 batch가 통과하면 finalize는 항상 통과한다. 체크리스트 C3-4가 기술하는 경로는 통합 테스트가 raw SQL로 상태를 구성해야만 재현된다 | C3-4 | 체크리스트/구현 불일치 |
| 발견-4 | (결번 — 인용하는 항목이 없음) | — | — |
| 발견-5 | 체크리스트 F-2 항목이 `external_import_step_entered`/`_abandoned` 두 개만 지목하지만 제품은 `_parse_completed`·`_parse_failed`·`_desktop_recommended`·`_finalized`를 포함해 여섯 개를 보낸다. 나머지 넷의 속성(`import_provider`, `import_failure_reason`)은 항목 문구가 덮지 않는다. 실측상 여섯 개 모두 content-free 요건은 충족 | F-2 | 체크리스트 범위 |
| 발견-6 | flag OFF에서 import 전용 「전체 내보내기」 버튼이 사라진다. 이 버튼이 flag로 감싸인 대화 목록 섹션 안에 있기 때문이다(`ExternalImportManagement.tsx:499`). §15의 rollback 요건은 계정 전체 데이터 내려받기가 Data 탭에 남아 가져온 기록을 포함하므로 실질적으로 충족되지만, import 화면만 보는 사용자에게는 export 수단이 없어 보인다 | E-1 · G-1 | UX |
| 발견-7 | **release blocker.** Google Takeout ZIP의 모든 항목이 data descriptor(general-purpose flag bit 3)를 쓰므로 local header의 크기가 0이고, fflate 스트리밍 `Unzip`이 그 값을 그대로 읽어(`externalImportWorker.ts:191`) 5.5 MB 대화 JSON이 `skip: "empty"`로 버려진다(`externalImportArchive.ts:138-140`). 파싱된 항목이 없고 Takeout에 항상 있는 `Takeout/Gemini/*.html` 때문에 `sawHtmlEntry`가 켜져, JSON으로 올바르게 내보낸 사용자에게 "HTML로 내보냈으니 JSON으로 다시 내보내라"고 안내한다. 정상 경로로 내보낸 사용자 전원이 실패한다 | H-2 | 결함 |
| 발견-8 | viewer가 저장된 markdown을 렌더링하지 않는다. 메시지는 `whitespace-pre-wrap` 평문으로 그려지고(`ExternalConversationViewer.tsx:339-343`) `components/imports/`에 Markdown 컴포넌트 참조가 없어, 표는 파이프 문자로 코드 블록은 백틱으로 보인다. 정책 §4가 평문과 제한 Markdown 양쪽을 허용하므로 정책 위반이 아니라 체크리스트 H-8 문구와 구현의 불일치이며, 문구를 정책에 맞추는 방향으로 결정됨 | H-8 | 체크리스트/구현 불일치 |
| 발견-9 | 분기 중복 메시지 안내가 범위를 밝히지 않는다. 수치는 export 전체 기준으로 정확하지만(선택을 바꿔도 불변) 문구에 그 사실이 없고, 「포함되지 않는 항목」 헤딩 아래에서 유일하게 「각 분기에 저장됩니다」로 포함을 말한다(`locales/ko.ts:1122-1123`, en도 동일 구조) | H2-2 | UX/문구 |
| 발견-10 | Gemini 경로 지역화 검증이 한국어 계정으로만 수행됐다. §3.1은 Takeout의 경로·파일명·라벨이 계정 언어로 번역된다고 정하지만, 영어 경로 Takeout 계정이 없어 ko/en 동일 결과를 확인하지 못했다 | H-2 (ko/en) | 미검증 범위 |
| 발견-11 | flag 변경에 전용 audit action이 없다. Admin Console에서 external import flag를 켜면 `app_settings.update_started`와 `app_settings.guest_default_model.updated` 두 건만 남고, `setExternalImportEnabled`를 포함한 flag 4종은 자체 이벤트를 기록하지 않는다(`app-settings/route.ts:107,122,128`). 값 자체는 두 이벤트의 metadata에 요청 body 전체가 담겨 확인되지만, **flag를 켠 사실이 「기본 모델을 바꿨다」는 이름으로 기록된다.** 나중에 "누가 언제 외부 import를 켰나"를 audit에서 이름으로 찾을 수 없다 | B-1 | 관측/audit |
| 발견-12 | 계정 설정 Data 탭의 사용량이 1 MB 미만을 전부 「0.0 MB」로 표시한다. `AuthButton.tsx:1743`이 MB로 고정 계산하고 `importFormatting.ts`의 `formatBytes()`를 쓰지 않기 때문이다. 같은 시점 관리 화면은 34.6 KB로 올바르게 표시하므로 **두 화면이 같은 값을 다르게 말한다.** 저장량이 적은 계정에서는 항상 0.0 MB로 보여 사용량 요약이 정보를 주지 못한다 | B-2 | 결함(표시) |
| 발견-13 | 대화 선택 화면의 요약이 선택분의 **메시지 합계**를 말하지 않는다. 「대화 190개 선택 · 약 1.5 MB」처럼 대화 수와 바이트만 있고 메시지 수는 대화별 행에만 있다. quota는 대화 수·메시지 수·바이트 세 축에 함께 걸리므로(§5.3), 메시지 상한(100,000)에 먼저 닿는 계정은 화면에서 그 사실을 읽을 수 없다 | B-4 | UX |
| 발견-14 | external import 운영 카운터가 하루만 보존된다. `EXTERNAL_IMPORT_COUNTER_PERIOD`가 `"external-import-day"`(`externalImportMetrics.ts:35`)인데 maintenance의 usage bucket 정리가 `period LIKE '%day%' AND periodStart < DATE_TRUNC('day', NOW())`를 삭제하므로(`maintenance.ts:536-539`), UTC 자정 이후 첫 실행에서 전날 버킷이 사라진다. `GET /api/admin/external-imports`는 최대 90일 창을 받고 `periodStart >= dayStartUtc(since)`로 합산하지만 실제로는 **항상 당일치만** 반환하며 그 사실을 말하지 않는다. 2026-08-17에 2였던 `quota_rejected`가 08-18에 0으로 보고됨. quota 거부와 staging sweep은 집계할 행을 남기지 않는 두 결과여서(`externalImportMetrics.ts:19-23`) 카운터가 사라지면 대체 기록이 없다 | F-1 · F절 sweep | 관측 결함 |
| 발견-15 | 계정 삭제가 재인증 창 밖에서 거절될 때 사용자가 그 사실을 알 수 없다. 세션이 step-up 창(기본 30분, `adminReauthenticationCore.ts:6-11`)을 벗어나면 `DELETE /api/user/account`가 428 `ACCOUNT_REAUTHENTICATION_REQUIRED`를 반환하고 아무것도 예약하지 않는데, client는 toast를 띄운 직후 `signOut()`으로 화면을 이동시키며(`AuthButton.tsx:993-999`) 도착한 로그인 페이지에는 사유가 없다. 이중 확인을 마치고 로그아웃까지 된 사용자는 삭제가 접수됐다고 믿지만 `accountStatus`는 `active` 그대로. 2026-08-18 E-3 실행 중 실제로 이 경로를 밟았고 안내를 보지 못함. 데이터가 지워지지 않는 방향의 실패라 안전하지만, 사용자가 삭제됐다고 믿는 상태로 남는 것이 문제 | E-3 | UX |
| 발견-16 | 삭제 대기 계정의 로그인 차단이 「코드가 무효하거나 만료됨」으로 잘못 표시된다. 이메일 코드 로그인은 `authorize()`에서 코드를 먼저 소비하고(`lib/auth.ts:93` → `emailLogin.ts:329-330` `consumedAt`), 그 뒤 `signIn` 콜백이 `pending_deletion`을 보고 거부한다(`lib/auth.ts:158`). 사용자에게 도달하는 문구는 「That code is invalid or expired. Request a new one.」이어서, 안내를 따라 새 코드를 받아도 같은 결과가 반복되고 매번 코드가 소진된다. 일일 요청 한도 12회(`emailLogin.ts:28`)와 30분 내 5회 실패 잠금(`:16`)에 걸릴 수 있다. 서버에는 `auth.sign_in_denied_pending_deletion` 감사 이벤트가 남으므로 운영자는 진실을 볼 수 있으나 사용자는 알 수 없다. 2026-08-18 E-3 실행 중 새 코드 2개로 재현 | E-3 | UX / 안내 |
| 발견-17 | 1인 운영 환경에서 Admin Console의 사용자 영구 삭제(`user.delete`)를 실행할 수 없다. `runWithAdminApproval`이 승인 청구를 요구하고(`lib/adminApproval.ts:121-139`), 승인 API가 `requestedById === session.user.id`를 무조건 409로 거부한다(`app/api/admin/approvals/route.ts:174-178`). 두 번째 관리자가 없으면 청구만 쌓이고 실행되지 않는다. 릴리스 게이트 registry의 `approvalPolicy.soleApproverAllowed`에 해당하는 예외가 이 경로에는 없다. 이중 통제 자체는 의도된 안전장치이나, 현재 조직 구성에서 도달 불가라는 사실이 운영자에게 드러나 있지 않다 — 즉시 삭제가 필요한 상황(예: 삭제 요구 대응)에서 처음 알게 된다. 2026-08-18 E-3 실행 중 승인 `cmsyk4pez0c4n02nzcv6q3iy8` 대기 상태로 확인 | E-3 | 운영 |

## 서명

| 항목 | 값 |
|---|---|
| 실행자 서명 | @mposition |
| 실행 완료일 (ISO 8601) | 2026-08-18 |
| 승인자 서명 | @mposition |
| 승인일 (ISO 8601) | 2026-08-18 |

