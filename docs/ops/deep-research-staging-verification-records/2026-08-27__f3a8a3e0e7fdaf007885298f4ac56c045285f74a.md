---
record: staging-verification
checklist: docs/ops/deep-research-staging-checklist.md
templateRevision: 2026-08-27b
environment: production
deploySha: f3a8a3e0e7fdaf007885298f4ac56c045285f74a
startedAtUtc:
completedAtUtc:
executor: mposition
approver: mposition
result: 통과
frozen: false
digest:
---

# 딥 리서치 확장 staging 검증 실행 — 2026-08-27 / f3a8a3e

> **이 문서는 에이전트가 만든 초안입니다.** 실행자가 보고한 관측만 옮겨 적었고,
> 보고되지 않은 값은 비워 두었습니다(기록 README 5번). 판정과 서명은 실행자가
> 준 것을 그대로 적었습니다. **아래 `(미보고)` 칸을 채운 뒤 commit 합니다.**

## 실행 환경

| 항목 | 값 |
|---|---|
| 환경 | **production** (staging 아님 — 아래 참조) |
| 배포 SHA (전체 40자리) | `f3a8a3e0e7fdaf007885298f4ac56c045285f74a` |
| production SHA와 동일한가 | 예 — 이 회차가 production에서 실행됨 |
| template revision | 2026-08-27b |
| 시작 (UTC) | (미보고) |
| 종료 (UTC) | (미보고) |
| 실행자 | mposition |
| 계정 플랜 (Pro 필요) | Pro (딥 리서치 실행이 성립했으므로) |
| 계정의 모델 상한 (`limits.maxModels`) | (미보고) |

**이 회차는 staging이 아니라 production에서 실행됐습니다.** 체크리스트 이름이
`staging`인 것은 이 문서군의 관례이고, 실제 실행 환경은 위에 적힌 대로입니다.
배포 시각은 `2026-08-27T06:18:39Z`(`/api/build-info`의 `deployedAt`)이며, 회차는
그 이후입니다.

## 실행 전 대화 준비

| 구획 | 선택한 모델 (이름) | 개수 | 상한과 같은가 |
|---|---|---|---|
| A · B | (미보고) | (미보고) | (미보고) |

## 사용한 질문

| 용도 | 실제로 보낸 문장 | 체크리스트와 동일한가 |
|---|---|---|
| 제안이 떠야 함 | `2026년 전고체 배터리 시장의 최신 자료를 비교해서 정리해줘` | 예 |
| 뜨면 안 됨 | (미보고 — 실행자가 "제안이 뜨면 안 되는 것 또한 확인"이라고 보고) | (미보고) |

## 항목별 결과

| 구획 | 항목 | 결과 | 증거 | 후속 티켓 |
|---|---|---|---|---|
| A | A-1 배포 SHA 기록 | pass | `/api/build-info` = `f3a8a3e…`, `deployedAt 06:18:39Z` | |
| A | A-2 상한 상태에서 제안 카드까지 | pass | 실행자 보고 | |
| A | A-3 답변 완료 후에만 표시 | pass | 카드 스크린샷 (문구·색·시간/크레딧 줄 확인) | |
| B | B-1 교체 다이얼로그 표시 | pass | 실행자 보고 | |
| B | **B-2 고른 모델이 빠지고 나머지는 남음** | **pass** | 실행자 보고 — **모델 이름 (미보고)** | |
| B | B-3 취소 시 실행 없음·선택 유지 | 미기록 | | |
| C | C-1 · C-2 상한 미만 경로 | 미기록 | | |
| D | D-1 제안이 뜨면 안 되는 질문 | pass | 실행자 보고 | |
| D | D-2 거절 후 같은 질문 재요청 | pass | 실행자 보고. `refusal=resolved`가 설계된 동작 | |
| D | D-3 딥 리서치 이미 선택된 대화 | 미기록 | | |
| E | E-1 · E-2 게스트·Free | 미기록 | | |
| F | F-1 접근성·문구 | 부분 | 카드 스크린샷에서 문구 확인. 보조기술 확인은 (미보고) | |

## B-2: 무엇을 골랐고 무엇이 남았는가

**기록 README 6번이 이름을 요구하는 칸입니다.** 판별 대상이 "무엇이 사라졌는가"가
아니라 "**고른 것이** 사라졌는가"이므로, 이름 없이는 그 판별이 일어났는지 나중에
확인할 수 없습니다. 실행자가 통과로 판정했으나 이름은 보고되지 않았습니다.

| 항목 | 값 |
|---|---|
| 교체 다이얼로그에 보인 모델 (전체) | (미보고) |
| **고른 모델** (빠져야 하는 것) | (미보고) |
| 실제로 사라진 패널 | (미보고) |
| **남은 모델과 그 답변이 그대로인가** | 예 — 실행자 보고 |
| 딥 리서치 패널이 새로 나타났는가 | 예 — 스크린샷 |

## B-3: 취소

미기록. 이번 회차에서 실행하지 않았습니다.

## 사용한 크레딧

| 구획 | 모델 | turn 수 | 카드 예상 크레딧 | 실제 정산 크레딧 |
|---|---|---|---|---|
| A-2 | (미보고) | 1 | — | (미보고) |
| B-2 | perplexity/sonar-deep-research | 1 | **30** (카드 표시, 스크린샷) | (미보고) |
| 합계 | | | | (미보고) |

## 이 회차에서 함께 확인된 것 — 체크리스트 밖

실행자가 회차 도중 **결함 두 건**을 발견했고, 둘 다 이 회차 안에서 수정·배포까지
끝났습니다. 체크리스트 항목은 아니지만 이 회차의 산출물이므로 기록합니다.

| 발견 | 내용 | 처리 |
|---|---|---|
| 문서 오류 | 체크리스트가 딥 리서치 1 turn을 16크레딧으로 적었음. 실제 30이며 대화 길이에 따라 최대 90 | #1116 (문서 수정, revision `2026-08-27b`) |
| 제품 결함 | 확장이 **이미 답한 모델에게도** 같은 질문을 다시 보내 중복 답변과 중복 과금이 발생 | #1118 (develop) · #1119 (main). `f3a8a3e`로 배포됨 |

두 번째 건의 수정 후 동작 — **확장 시 기존 모델 패널이 다시 답하지 않음** — 을
실행자가 이 배포에서 확인했습니다.

## 판정

| 항목 | 값 |
|---|---|
| 결과 (통과 / 조건부 / 실패) | **통과** |
| 조건부일 때의 조건 | 해당 없음 |
| 건너뛴 구획과 그 이유 | B-3 · C · D-3 · E는 선택 구획이며 이번 회차에서 실행하지 않음. 체크리스트의 필수 3항목(A-2 · B-2 · B-3) 중 B-3만 미실행 |
| 발견 사항 | 위 "체크리스트 밖" 두 건. 둘 다 수정·배포 완료 |
| 후속 티켓 | 딥 리서치 패널에 같은 질문이 두 번 보이는 표시 문제 — 확장이 새 user 메시지를 만들기 때문. 미착수, 차단 아님 |

**차단 항목 B-2가 통과이므로 이 회차는 실패가 아닙니다.** 다만 B-2의 모델 이름이
비어 있어, 기록 README 6번이 요구하는 형태를 아직 갖추지 못했습니다.

## 정리 의무

| 항목 | 값 |
|---|---|
| staging 대화 삭제 (UTC) | (미보고) |
| 소모 크레딧 확인 (UTC) | (미보고) |

## 서명

| 항목 | 값 |
|---|---|
| 실행자 서명 | mposition |
| 실행 완료일 (ISO 8601) | 2026-08-27 |
| 승인자 서명 | mposition |
| 승인일 (ISO 8601) | 2026-08-27 |

한 사람이 실행자와 승인자를 겸하는 것은 registry에 기록된 허용입니다
(`approvalPolicy.soleApproverAllowed`, `AGENTS.md`).
