---
record: staging-verification
checklist: docs/ops/deep-research-staging-checklist.md
templateRevision: 2026-08-27b
environment:
deploySha:
startedAtUtc:
completedAtUtc:
executor:
approver:
result:
frozen: false
digest:
---

# 딥 리서치 확장 staging 검증 실행 — <날짜> / <deploy SHA>

## 실행 환경

| 항목 | 값 |
|---|---|
| 환경 | staging / production / 기타 |
| 배포 SHA (전체 40자리) | |
| production SHA와 동일한가 | |
| template revision | 2026-08-27b |
| 시작 (UTC) | |
| 종료 (UTC) | |
| 실행자 | |
| 계정 플랜 (Pro 필요) | |
| 계정의 모델 상한 (`limits.maxModels`) | |

## 실행 전 대화 준비

**실행 전에** 어느 모델 조합으로 실행할지 적습니다. B 구획은 **상한을 채운
상태**에서만 성립하므로, 선택한 모델 수가 상한과 같아야 합니다.

| 구획 | 선택한 모델 (이름) | 개수 | 상한과 같은가 |
|---|---|---|---|
| A · B | | | |
| C (선택) | | | |

## 사용한 질문

체크리스트의 문장을 그대로 썼는지, 바꿨다면 무엇으로 바꿨는지 적습니다.

| 용도 | 실제로 보낸 문장 | 체크리스트와 동일한가 |
|---|---|---|
| 제안이 떠야 함 | | |
| 뜨면 안 됨 (선택) | | |

## 항목별 결과

체크리스트의 A–F 구획을 그대로 옮기고, 각 항목에 다음 중 하나를 적습니다.

- `pass` — 확인함. 증거 참조를 함께 적습니다.
- `fail` — 확인했고 실패했습니다. 후속 티켓을 적습니다.
- `n/a` — 이 실행에서 해당하지 않습니다. 이유를 적습니다.
- `미기록` — 실행하지 않았거나 기록이 없습니다. **나중에 채우지 않습니다.**

| 구획 | 항목 | 결과 | 증거 | 후속 티켓 |
|---|---|---|---|---|
| A | A-1 | | | |
| A | A-2 | | | |
| A | A-3 | | | |
| B | B-1 | | | |
| B | B-2 | | | |
| B | B-3 | | | |

증거는 스크린샷 경로, 오류 코드, 소요 시간처럼 **다시 확인할 수 있는 것**을
적습니다. **딥 리서치 답변 본문은 증거로 저장소에 넣지 않습니다.**

## B-2: 무엇을 골랐고 무엇이 남았는가

이 구획의 판별 대상입니다. 이름을 적지 않으면 판별이 일어났는지 알 수 없습니다.

| 항목 | 값 |
|---|---|
| 교체 다이얼로그에 보인 모델 (전체) | |
| **고른 모델** (빠져야 하는 것) | |
| 실제로 사라진 패널 | |
| **남은 모델과 그 답변이 그대로인가** | |
| 딥 리서치 패널이 새로 나타났는가 | |

## B-3: 취소

| 항목 | 값 |
|---|---|
| 취소 방법 (Esc / 배경 클릭 / 취소 버튼) | |
| 딥 리서치가 시작되지 않았는가 | |
| 세 모델이 그대로인가 | |
| 크레딧이 소모되지 않았는가 | |

## 사용한 크레딧

거절·취소 경로는 0크레딧입니다. 유료는 A-2(일반 답변)와 B-2(딥 리서치)이고,
선택 구획을 하면 C-1·D-1이 더해집니다.

딥 리서치는 **고정 30크레딧이 아니라** 기본 30에 입력 길이 배수가 곱해진
값입니다(체크리스트의 배수 표). 그래서 **카드에 뜬 예상치와 실제 정산값을 함께**
적습니다.

| 구획 | 모델 | turn 수 | 카드 예상 크레딧 | 실제 정산 크레딧 |
|---|---|---|---|---|
| A-2 | | | | |
| B-2 | | | | |
| C-1 (선택) | | | | |
| D-1 (선택) | | | | |
| 합계 | | | | |

## 판정

| 항목 | 값 |
|---|---|
| 결과 (통과 / 조건부 / 실패) | |
| 조건부일 때의 조건 | |
| 건너뛴 구획과 그 이유 | |
| 발견 사항 | |
| 후속 티켓 | |

**B-2가 `fail`이면 결과는 실패이고 릴리스 차단입니다.** B-1·B-3·A-3의 실패는
조건부입니다. 체크리스트 마지막 구획이 그 이유를 적어 두었습니다.

## 정리 의무

이 회차는 staging 계정에 대화를 남기고 크레딧을 소모합니다. R2 object는
만들지 않습니다.

| 항목 | 값 |
|---|---|
| staging 대화 삭제 (UTC) | |
| 소모 크레딧 확인 (UTC) | |

## 서명

| 항목 | 값 |
|---|---|
| 실행자 서명 | |
| 실행 완료일 (ISO 8601) | |
| 승인자 서명 | |
| 승인일 (ISO 8601) | |
