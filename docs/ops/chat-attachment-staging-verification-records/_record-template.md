---
record: staging-verification
checklist: docs/ops/chat-attachment-staging-checklist.md
templateRevision: 2026-08-22a
environment:
deploySha:
startedAtUtc:
completedAtUtc:
executor:
approver:
result:
frozen: false
digest:
---

# 채팅 첨부 형식 staging 검증 실행 — <날짜> / <deploy SHA>

## 실행 환경

| 항목 | 값 |
|---|---|
| 환경 | staging / production / 기타 |
| 배포 SHA (전체 40자리) | |
| production SHA와 동일한가 | |
| 다르다면 첨부 표면 diff 결과 | |
| template revision | 2026-08-22a |
| 시작 (UTC) | |
| 종료 (UTC) | |
| 실행자 | |
| `attachmentsEnabled` (시작 시점) | |
| staging R2 bucket이 production과 분리됨 | |

## 실행 전 파일 준비

**실행 전에** 어떤 파일로 실행할지 적습니다. 내용은 적지 않고 이름·크기·출처만
적습니다 — 판별 대상은 경계와 파서이지 내용이 아닙니다.

`.doc`·`.xls`·`.ppt`의 **출처**는 반드시 적습니다. §C는 "LibreOffice가 만든
fixture가 아닌 진짜 Microsoft Office 파일"을 판별하는 구획이고, 출처를 적지
않으면 그 판별이 일어났는지 나중에 알 수 없습니다.

| 구획 | 파일 (이름 · byte) | 출처 (무엇이 저장했는가) |
|---|---|---|
| B | | |
| C | | |
| D | | |
| E | | |

## 항목별 결과

체크리스트의 A–H 구획을 그대로 옮기고, 각 항목에 다음 중 하나를 적습니다.

- `pass` — 확인함. 증거 참조를 함께 적습니다.
- `fail` — 확인했고 실패했습니다. 후속 티켓을 적습니다.
- `n/a` — 이 실행에서 해당하지 않습니다. 이유를 적습니다.
- `미기록` — 실행하지 않았거나 기록이 없습니다. **나중에 채우지 않습니다.**

| 구획 | 항목 | 결과 | 증거 | 후속 티켓 |
|---|---|---|---|---|
| A | | | | |

증거는 스크린샷 경로, 오류 코드, 응답 시간 측정값처럼 **다시 확인할 수 있는
것**을 적습니다. **첨부 파일 원문과 추출된 텍스트는 증거로 저장소에 넣지
않습니다** — "zip 안 6개 중 4개가 답에 나타남", "제외 2건이 토스트 숫자와
일치"처럼 관측만 적습니다.

## E 구획: 실제 기기

CI가 재현하지 못하는 유일한 이유가 기기이므로, **어느 기기·OS·브라우저인지**를
적지 않으면 이 구획은 실행되지 않은 것과 같습니다.

| 항목 | 값 |
|---|---|
| 기기 · OS 버전 | |
| 브라우저 · 버전 | |
| 파일 선택기가 보고한 MIME (비어 있었는가) | |

## 사용한 크레딧

업로드와 모든 거절 경로는 0크레딧입니다. 유료는 B-2·C-2·D-1·F-2 네 턴이고,
권장 최소(A·B·C·H)로 줄이면 B-2·C-2 두 턴입니다.

| 구획 | 모델 | turn 수 | 크레딧 |
|---|---|---|---|
| B-2 | | | |
| C-2 | | | |
| D-1 (선택) | | | |
| F-2 (선택) | | | |
| 합계 | | | |

## 판정

| 항목 | 값 |
|---|---|
| 결과 (통과 / 조건부 / 실패) | |
| 조건부일 때의 조건 | |
| 건너뛴 구획과 그 이유 | |
| 발견 사항 | |
| 후속 티켓 | |

B-2 · C-2 · E-1 중 하나라도 `fail`이면 결과는 **실패**입니다. 체크리스트
마지막 구획이 그 이유를 적어 두었습니다.

## 정리 의무

이 회차는 staging 계정에 대화와 첨부를 남기고, **R2 object도 남깁니다.**
finalize에 실패한 객체는 서버가 즉시 지우지만, 성공한 업로드는 남습니다.

| 항목 | 값 |
|---|---|
| staging 대화·첨부 삭제 (UTC) | |
| 게스트 ephemeral object 만료 확인 (UTC) | |
| 로컬 테스트 파일 정리 (UTC) / 확인자 | |

## 서명

| 항목 | 값 |
|---|---|
| 실행자 서명 | |
| 실행 완료일 (ISO 8601) | |
| 승인자 서명 | |
| 승인일 (ISO 8601) | |
